# 2026-03-07 工作记录

## 今天主要在做什么
今天主要推进的是：把“后端先行”这条路线从总规划，真正落到可以开始执行的地面上。

如果用一句话概括，这件事是在解决“项目知道大方向，但还没有具体施工图”的问题，目的是让后续能够按阶段稳定推进，而不是边做边改、边做边猜。

## 为什么今天先做这件事
当前项目正处于“旧仓 UI 还在继续完善，新仓后端可以先独立推进”的阶段，所以今天优先处理的是后端实施计划与 B1 地基建设，而不是直接去做缩略图、zip 浏览页面或真实 UI 迁移。

这样安排的原因是：
- 现在如果直接做最终功能，很容易把后端实现、桥接方式和未来 UI 接口混在一起，后面返工成本会很高。
- 当前仓库虽然已经有 Tauri 壳层和目录骨架，但很多 Rust 模块还只是“占位目录”，还不足以承载真实迁移。
- 只有先把共享模型、合同、工作区结构、CLI 骨架这些基础设施补起来，后面的数据库、扫描、zip、缩略图才有稳定落点。

## 今天推进了哪些关键点

### 1. 把总实施计划落成可执行的阶段计划
今天先处理了“计划太大、但执行颗粒度还不够”的问题。

这件事本身做了什么：新增了一份更具体的实施计划文档，把总纲里的后端先行路线拆成了 `B1` 到 `B4` 四个明确阶段，分别对应地基冻结、数据地基、扫描入库闭环、zip 与归档索引。

它的重要性在于：项目终于不再只是知道“以后要做哪些能力”，而是明确了“当前这一阶段先做什么、什么先不做、每一步验收什么”。

它对后续的帮助是：后面每次推进时，可以直接对照阶段目标执行，减少“方向没错，但顺序和颗粒度不稳”的风险。

### 2. 把 Rust 后端从占位目录推进成真实 workspace
今天还推进了 Rust `workspace`（把多个相关模块放在同一仓库里统一管理的结构）这一层的落地。

这一步解决的是：原来仓库里虽然已经有 `crates/app-core`、`crates/media-db`、`crates/media-io`、`crates/shared-model` 这些目录，但它们还只是空骨架，不能真正参与编译，也无法承载后端能力的逐步迁移。

今天的结果是：根目录已经补上统一的 workspace 配置，相关 crate 也从“只有 README 的目录”变成了“可以被 Cargo 管理、可以参与编译和测试的真实模块”。

它说明目前项目已经从“初始化骨架阶段”进入到了“可以开始做真实后端能力”的阶段。

### 3. 冻结第一批共享模型与 contracts
另外补上了 B1 阶段里最关键的一层：共享模型和合同。

这件事本身做了什么：在 Rust 侧建立了第一批共享类型，包括通用 ID、错误码、任务状态、任务进度、分页模型、基础媒体摘要；在 `packages/contracts` 里同步建立了 TypeScript/Zod 合同与样例 JSON，并补上了基础测试。

这件事虽然看起来不像“做出了功能”，但它的意义是：从现在开始，后续数据库、扫描、zip、缩略图这些能力不再各自发明一套命名和返回结构，而是围绕统一模型推进。

### 4. 明确当前阶段 Rust 质量验证是怎么做的
今天还顺手把一个很容易让人混淆的问题理顺了：在 Rust 开发里，当前这阶段的质量验证，并不是靠单一命令完成，而是分层进行的。

这一步解决的是：很多人熟悉前端开发，但不一定熟悉 Rust 的工程流程，容易把 `cargo fmt`、`cargo check`、`cargo test` 都看成差不多的事情。实际上它们分别承担的是格式检查、编译级验证、测试验证和冒烟验证，不是一回事。

目前我们已经明确，当前阶段主要依靠四层验证：
- `cargo fmt`：检查代码格式是否统一，更接近前端里的格式化检查。
- `cargo clippy`：做静态规则检查，更接近前端里的 `eslint`。
- `cargo check`：做编译级检查，确认整个工作区在依赖锁定后能通过编译。
- `cargo test`：执行 Rust 测试，覆盖单元测试、最小集成验证等内容。
- CLI harness / runtime smoke：做最小可运行性检查，作用上接近 Electron 里的冒烟测试。

它说明目前项目已经不是“写完代码看能不能跑”那种松散方式，而是在逐步形成一套有层次的质量门禁。

为了让非 Rust 背景的人更容易理解，目前可以先用下面这张对照表来建立直觉：

| Rust 命令 | 当前作用 | 可类比的前端命令 |
|---|---|---|
| `cargo fmt` | 检查代码格式是否统一 | `prettier --check` |
| `cargo clippy` | 静态规则检查与坏味道提示 | `eslint` |
| `cargo check` | 编译级验证，不产出最终发布包 | `tsc --noEmit` |
| `cargo build` | 真正构建产物 | `npm build` |
| `cargo test` | 运行测试 | `vitest` |
| CLI / smoke check | 检查最小链路能否运行 | Electron 冒烟测试 |

这里也形成了一个比较重要的判断：
- `cargo fmt` 不能简单理解成 `eslint`，它更像格式化检查。
- `cargo check` 也不能直接等于 `npm build`，它更像“先确认能不能编过”。
- 真正更接近 `eslint` 的是 `cargo clippy`。
- 真正更接近 `npm build` 的是 `cargo build`。

这件事对后续的帮助是：后面进入数据库、扫描、zip 等真实能力开发时，大家会更清楚每一层验证是在防什么问题，也更容易理解为什么要同时保留格式、静态检查、测试和冒烟这几类质量动作。

## 今天遇到的问题

### 1. Windows 下 Rust 校验不能直接按普通方式跑通
今天遇到的问题是：Rust 工作区在普通环境下执行完整检查时，SQLite 的 bundled 编译链路还是会受 Windows/MSVC 环境影响，直接跑会出现头文件路径不完整的问题。

它带来的影响是：如果忽略这件事，表面上看代码已经写好了，但真正到需要编译、测试或 CI 落地时，可能会在本地环境与工具链交界处卡住。

最后的处理方式是：继续使用仓库里已有的 `run-cargo-with-msvc.cmd` 包装脚本，通过 Visual Studio 开发者命令环境来执行 `clippy`、`test`、CLI harness 等检查。

这件事说明后续需要特别注意：在 Windows 桌面场景下，Rust 代码的“能写出来”和“能稳定构建验证”是两件事，工具链环境仍然要被当成正式工程条件来管理。

### 2. 工作区引入后，原有细节约束开始暴露出来
今天还发现：一旦把占位目录真正接入 workspace，很多以前不会立刻暴露的问题就会集中出现，比如 TypeScript 在 `NodeNext` 模式下要求显式导入扩展名，Rust 的 `clippy` 也会对字符串格式写法提出更严格要求。

目前的结论是：这不是坏事，反而说明仓库正在从“能放文件”进入“要按工程标准运行”的阶段。越早让这些约束暴露出来，后面就越不容易积累隐性技术债。

## 今天形成了哪些判断
- 已经确认：`B1` 的核心不是做功能，而是把后端开发地基真正搭起来。
- 暂时不做：缩略图主链路、zip 浏览最终接线、复杂 `src-tauri` 桥接。
- 优先级靠后：`media-thumb`、`media-playback`、sidecar 协议的真实实现。
- 原因是：如果先跳去做这些重能力，而数据模型、contracts、workspace、CLI 骨架还没稳定，后面很容易推倒重来。

## 当前项目处于什么状态
- 已经具备：统一的 Rust workspace、真实 crate 基线、第一批共享模型、contracts 初版、最小 CLI harness、fixtures 与 benchmarks 的目录约定。
- 仍然缺少：数据库 migration 体系、repository 实现、扫描入库闭环、zip 目录与 entry 读取能力。
- 还没有开始：缩略图 Rust 主链路、`rar/7z` 归一化、播放后端适配、字幕宿主协议。
- 下一步最自然的方向是：正式进入 `B2`，先把数据库 schema、migration、repository 做成稳定的数据地基。

## 这一天的工作意味着什么
如果从整个项目的推进角度来看，今天的工作意味着：MediaPlayerNext 这个仓库已经不再只是“一个能启动的新壳子”，而是在开始形成真正可持续推进的后端工程结构。

它不是在直接完成最终功能，而是在为后续数据库、扫描、zip、缩略图这些核心能力创造条件。换句话说，今天做的不是“把房间装修好”，而是先把承重墙、管线和施工顺序确定下来。只有这一步稳了，后面每一块能力才有可能在不反复返工的前提下接上来。
