# 2026-03-11 工作记录

## 今天主要在做什么
今天主要推进的是：把项目的 UI 路线从“等旧仓 theme 收口后再整体迁移”，正式改成“布局先行、样式链重建、面板内容分步接回”的新实施方式，并把这条新路线真正落成文档结构和第一批界面骨架。

如果用一句话概括，这件事是在解决“旧仓 UI 主题系统推进不顺，导致新仓 UI 迟迟无法开始”的问题，目的是让项目不再被旧 theme 收口节奏卡住，而是可以在新仓里按切片逐步建立自己的初始 UI 架构。

## 为什么今天先做这件事
当前项目已经不是“只有后端骨架”的状态，`I1` 的 repository / adapter 接线也已经有了首轮结果，所以今天优先处理的是 UI 路线切换和 `U0` 壳层落地，而不是继续写新的后端能力。

这样安排的原因是：
- 旧仓真正卡住的部分已经不是交互关系，而是 theme 系统和样式链条本身；如果继续等待，整个新仓 UI 会长期停在说明阶段。
- 新仓后端边界、repository 壳层和宿主协议已经足够稳定，已经具备“先搭新壳层，再逐步接面板”的条件。
- 只有先把文档、定义边界和 `U0` 的四容器骨架固定下来，后面 `U1/U2/U3` 才能按切片验收，而不是每次又回到“先等旧 UI 完整收口”的老问题上。

## 今天推进了哪些关键点

### 1. 正式把 UI 路线从“等待迁移”改成“分步重建”
今天先处理了项目层面的路线判断，而不是直接改页面样式。

这件事本身做了什么：先结合当前仓库状态重新判断，确认后端地基、`I1` 壳层和 repository 边界已经足够支撑 UI 前置；然后把原来“等旧 App theme 收口后再迁移”的思路，改成“先迁布局骨架、再重建样式链、再逐面板接内容”的新路线。

它的重要性在于：这一步不是简单改优先级，而是把项目从“被旧仓状态牵着走”推进到“新仓按自己的节奏建立 UI 架构”。

它对后续的帮助是：从现在开始，UI 工作可以像后端阶段一样拆成 `U0/U1/U2...` 这种可分段验收的切片，不需要再等一个“大一统 theme 系统”完全收完才开工。

### 2. 把 docs 目录重组，历史计划归档，当前入口收口
今天还推进了文档层面的收口。

这一步解决的是：原来的 `docs/` 根目录堆了多份阶段计划，随着项目方向变化，很容易让后续阅读的人分不清“哪些是历史方案，哪些是当前准绳”。

这件事本身做了什么：新建了根索引 `docs/README.md`，把原来根层的 `migration-plan` 和 `00` 到 `05` 这些历史计划整体移进 `docs/archive/root-plans/`，同时同步更新了仓库内相关路径引用和 `AGENTS.md` 的阅读入口。

它说明目前项目已经从“计划不断叠加”进入到了“历史计划归档、当前入口单一”的阶段。

### 3. 把“新项目定义”和“源项目已知数据”彻底拆开
另外补上了今天最关键的一层文档边界：不能让新项目定义继续夹带旧项目实现细节。

这件事本身做了什么：保留 `docs/ui/ui-definition.md` 作为 `MediaPlayerNext` 的正式 UI 定义文档，只写新项目决定采用的层级、变量、回落规则、切片和验收标准；同时新增 `docs/ui/source-ui-known-data.md`，把 `MediaPlayerX` 里和 `1.0 背景层`、`2.0 共享壳层` 相关的已知字段、代码锚点、文档位置、命名漂移和当前实施共识单独记录下来。

它的重要性在于：如果这两类信息继续混写，新项目文档会越来越像“旧仓考古笔记”，而不是“从零重建的正式定义”。

它对后续的帮助是：后面继续补 `Header / Sidebar / Main / Metadata` 派生层时，可以一边查旧仓已知事实，一边保持新仓定义清洁，不会让 theme 基础文档越写越乱。

### 4. 正式开始 U0，把四大容器骨架和共享壳层样式落进桌面壳
今天最直接的代码推进，是把 `U0` 的第一轮实现真正做出来。

这件事本身做了什么：在桌面壳里把页面结构改成 `bg.app.root`、`fg.header.root`、`fg.sidebar.root`、`fg.main.root`、`fg.meta.root` 这五个稳定层级；同时在前端样式里建立了第一批 `--mpx-*` 变量，包括背景层、共享 frame、圆角、留白和分隔宽度，并做了桌面到窄屏的基础响应式规则。

它的重要性在于：项目终于不再只是“文档里说要有背景层和四大容器”，而是已经有了一个真实可运行、能看见层级分隔、能承载后续面板接回的 UI 壳层。

它对后续的帮助是：接下来 `U1` 进入 `LibraryPanel / ScanPanel` 时，不需要再先搭页面骨架，而是直接在已经稳定的 `Sidebar / Main / Metadata` 容器里接内容。

### 5. 把 U0 的状态回写到正式定义文档，并完成前端构建验证
今天还补了两件看起来像收尾、其实很重要的工作。

这件事本身做了什么：一方面把 `docs/ui/ui-definition.md` 里的 `U0` 状态从“待开始”更新成“已完成首轮”，把这次实际落地的骨架和变量合同写回文档；另一方面重新执行了 `npm run build:web`，确认这轮 UI 壳层调整在前端构建上是稳定的。

它的重要性在于：今天的工作不是只在本地拼了一个能看的页面，而是把“定义 -> 实现 -> 验证”三件事闭成了最小回路。

它对后续的帮助是：后面每推进一个 UI 切片，都可以沿用今天这个模式——先写定义，再做实现，再跑最小验证，而不是页面改了很多、文档却还停在旧状态。

## 今天遇到的问题

### 1. U0 第一轮落地后，打开应用看到的是“说明”，不是“真正的布局占位”
今天遇到的问题是：虽然 `U0` 已经把背景层和四大容器骨架做出来了，但当前各容器里的内容仍然偏“说明性文字”和开发期验证信息，看起来更像阶段说明页，而不是正式的初始界面。

它带来的影响是：从实现角度看，这一轮确实验证了容器分隔和样式变量；但从使用者直觉看，打开应用后会觉得“这不是布局，而是在解释布局”。

最后的处理方式是：先确认这属于 `U0` 首轮验证的正常结果，不是样式接错；同时也明确这不应作为长期默认界面。下一步进入 `U0` 收口或 `U1` 前，应该把这些长说明收敛成更轻的占位块、空态和标题结构。

这件事说明后续需要特别注意：开发期验证内容可以暂时存在，但不能长期占据主界面，否则项目会从“搭骨架”滑向“把说明页当产品壳”。

### 2. 需要澄清 Tauri 开发态的重启规则，避免把正常热更新误当成异常
今天还发现一个容易让后续操作变笨重的问题：并不是每次改完代码都需要重新执行 `npm run tauri:dev`。

目前的结论是：如果只是前端 React/CSS 改动，开发态通常会通过 Vite 热更新直接生效；只有在宿主配置、依赖、环境或 Tauri/Rust 层出现变化时，才更可能需要重启开发进程。这件事虽然不是业务功能，但会直接影响后续 UI 迭代效率。

## 今天形成了哪些判断
- 已经确认：当前 UI 工作可以脱离旧仓 theme 收口节奏，按切片独立推进。
- 已经确认：`ui-definition.md` 必须只写新项目定义，源项目资料必须独立存档。
- 已经确认：`U0` 的第一轮目标是验证背景层、共享壳层、四大容器和回落链，不是直接做最终可用界面。
- 暂时不做：单容器派生变量的大规模细化、真实业务面板内容接回、视觉等价复刻。
- 原因是：当前最重要的是先把 UI 基础架构和 theme 责任边界稳住，再进入 `U1` 的具体面板切片。

## 当前项目处于什么状态
- 已经具备：新的 UI 文档入口、源项目已知数据档案、`U0` 的背景层与四大容器骨架、首轮共享壳层变量、前端构建验证结果。
- 仍然缺少：更像正式界面的 U0 空态占位收口，以及 `Header / Sidebar / Main / Metadata` 的单容器派生层细化。
- 还没有开始：`U1` 的真实 `LibraryPanel / ScanPanel` 接回，`U2` 之后的列表、归档、详情等页面内容切片。
- 下一步最自然的方向是：先把 `U0` 从“说明性壳层”收成“结构型壳层”，再进入 `U1` 的左侧面板接入。

## 这一天的工作意味着什么
如果从整个项目的推进角度来看，今天的工作意味着 UI 终于从“需要等待的后置事项”，变成了一个可以和后端一样按阶段推进的正式主线。

它不是在直接完成最终功能，而是在为后续 theme 系统、面板迁移和逐步验收创造一套清晰的结构基础。换句话说，今天真正完成的不是一个漂亮页面，而是“以后每次做 UI 时都该按什么边界、什么节奏、什么入口继续往前做”的第一套新规则。

## 补充更新：把通用组件定义正式落到 3 / 4 / 5 编号

### 6. 先把大面板、小面板、按钮三类基础件编号固定下来
继续推进 UI 基础定义时，今天又把后续最容易反复漂移的三类基础件正式编号了。

这件事本身做了什么：把新项目通用组件定义收成 `3 大面板层`、`4 小面板层`、`5 按钮层`；明确 `4 小面板层` 完全独立，不继承 `3 大面板层`；同时把 `5.1 通用变体` 固定成整个 app 所有按钮的默认根样式。为了避免新项目定义继续掺杂旧仓实现，我还把源项目关于大面板、小面板、按钮的已知锚点整理进了独立的参考文档。

它的重要性在于：到这一步，theme 基础不再只是“先大概有个背景层和容器层”，而是已经开始具备可持续扩展的层级骨架。后面再做设置面板、帮助面板、小对话框和按钮覆盖时，就不需要一边做一边重新讨论“它到底属于哪一层”。

它对后续的帮助是：后续无论是做真实 `Settings`、`Help`，还是补 `Library / Scan / Detail` 这类面板的动作按钮，都可以直接挂到已经固定的 `3/4/5` 结构上，而不是再开新的临时样式分支。

## 补充更新：最小设置面板已经接到 Header

### 7. 先把设置入口和最小界面设置链路跑通
在通用组件定义固定后，今天继续把第一个真实 UI 动作接了起来：从 Header 打开设置面板。

这件事本身做了什么：先从源项目提取设置按钮的 SVG 定义，放进新仓 Header；然后接了一个最小 `Settings` 大面板，当前 side 只保留一个分页“界面设置”，main 只开放四个最基础的界面项：面板背景遮罩透明度、容器外边界系数、容器内边距系数、分割条宽度系数。与此同时，这四项都不只是静态表单，而是已经真正开始回写到当前页面的 CSS 变量上。

它的重要性在于：这说明新仓 UI 已经不再停留在“壳层摆好了，但还没有真实交互入口”的状态。至少现在已经有了一条从主界面进入面板、从面板调参数、再即时看到页面变化的最小闭环。

它对后续的帮助是：后面继续扩设置面板时，不需要再先从“入口按钮、遮罩、大面板骨架、分页结构、变量回写”这些基础工作重新开始，而是可以直接在现有最小闭环上增量扩展。

## 补充更新：主页面固定布局与分割条调节已初步成型

### 8. 把主界面从说明页收成固定三栏壳，并加入可拖拽分割条
在最小设置面板能打开之后，今天继续把主页面收成了真正的初始桌面壳，而不是说明页。

这件事本身做了什么：先把 Header 收成只保留设置按钮，把原来大段解释性内容移出主界面；再把页面固定成 `header + side | main | meta` 的稳定三段结构，让三列占满剩余高度，同时避免 root 级滚动条出现。随后又补了两个可拖拽分割条，用于调整 `Sidebar/Main` 和 `Main/Metadata` 的宽度分配，并把拖拽结果也纳入了会话级持久化。推进过程中还顺手修正了一个职责错误：`splitter width` 现在只影响三列之间的横向间距，不再错误影响 Header 和工作区之间的纵向间距。

它的重要性在于：这一步意味着“前端主界面初始布局完成”终于可以成立。现在虽然内容还是占位，但页面骨架、三栏分配、设置入口、参数调节、分割条拖拽和刷新后保留都已经在同一个壳里闭合起来了。

它对后续的帮助是：接下来最自然的工作就不再是继续改骨架，而是开始把 `Sidebar` 的媒体库 / 扫描、`Main` 的列表 / 归档、`Metadata` 的详情这三块真实面板逐步接回。换句话说，今天之后前端主界面的基础建设阶段已经可以暂时告一段落，后面可以正式进入面板内容切片阶段。

## 补充更新：当前对 UI 阶段的判断

### 9. 当前结论已经从“U0 首轮落地”推进到“主界面初始布局完成”
- 已经确认：主页面固定三栏、设置入口、最小设置面板、四项界面系数调节、分割条拖拽与会话保留这几块已经形成了可运行的第一版桌面 UI 壳。
- 已经确认：当前阶段的重点应该从“继续打磨壳层解释信息”切换到“逐步接真实面板内容”。
- 当前仍未完成：设置面板持久化到正式配置、更多设置分页、真实业务内容、帮助面板以及更细的视觉收口。
- 结论是：可以把“前端主界面初始布局完成”作为当前阶段的阶段性里程碑，但还不能把它等同于“前端可用功能完成”。

## 补充更新：Header Logo 入口与导入任务大面板骨架

### 10. 把 Header 从单按钮入口推进到 Logo + Settings 的稳定结构
这一轮继续在现有桌面壳上推进，不再回到说明页方向。

这件事本身做了什么：先把 `Header` 改成左侧 `Logo`、右侧设置按钮的稳定结构；`Logo` 按钮文案固定为 `MediaPlayerNext`，并补了 `idle/open` 两态。随后接入了一个独立的大面板 overlay，当前以 `导入任务` 作为首轮骨架实例，沿用大面板层而不是另开一套局部弹层结构。

它的重要性在于：`fg-header-root` 不再只是“顶部有个设置按钮”的最小壳，而是开始承接真正的全局入口职责；同时 `fg-import-task-root` 也已经在新仓里有了可运行的第一版大面板落点。

它对后续的帮助是：后面要继续补 `busy` 状态、导入摘要、扫描状态、帮助面板或更多全局入口时，不需要再重做 Header 与 overlay 骨架。

## 补充更新：side / main / meta 三列都已细化为 header / main / footer

### 11. 三列结构从统一占位块推进到稳定三段容器
这一轮最关键的结构推进，是把 `Sidebar / Main / Metadata` 都从统一占位块收成了同一口径的三段结构。

这件事本身做了什么：在三列中分别补齐 `header / main / footer` 子结构与对应 slot，让每一列都有稳定的顶部区、中部区、底部区；与此同时继续保留当前占位内容，但不再让内容结构决定容器框架。

它的重要性在于：从这一刻开始，`U1/U2/U3/U4` 后续真实内容接入时，已经不再是“往一个大占位块里填东西”，而是可以直接挂到已经固定的三段容器里。

它对后续的帮助是：三列的 header/footer 可以继续承接动作、摘要、分页、caption 等稳定职责，真实业务主要集中进入 `main` 区域，整体结构会更可维护。

## 补充更新：三列垂直对齐与新的间距调节项

### 12. 统一三列 header / footer 的高度与基线，并新增“容器内上中下间距系数”
在三段结构建立之后，这一轮继续把纵向节奏收紧。

这件事本身做了什么：先把 `Sidebar / Main / Metadata` 三列统一改成固定的 `header / main / footer` 三行模板，让 `header/footer` 保持基本固定高度，`main` 成为主要伸缩区；再在设置面板的“界面设置”中新增 `容器内上中下间距系数`，当前范围 `0~2`，默认值 `1`，并按会话级方式保留。需要特别说明的是，这条设置项在新仓当前只用于控制三列大容器中 `header -> main -> footer` 的纵向间距，不下沉影响 `main` 内部内容的间距。

它的重要性在于：这一步把“主界面三列已经成型”继续推进成了“主界面三列开始具备稳定节奏和统一基线”。这不只是视觉细化，而是在给后续内容接入建立更稳定的承载约束。

它对后续的帮助是：后面继续接 `LibraryPanel / ScanPanel / Items / Metadata` 时，可以把注意力集中在面板内容本身，而不需要反复调整三列外层的纵向关系。

## 补充更新：当前阶段判断继续前移

### 13. 当前不再只是“主界面初始布局完成”，而是开始进入可承载 U1 的结构细化阶段
- 已经确认：`Header` 的全局入口结构、大面板实例骨架、三列 `header / main / footer` 子结构和纵向节奏控制都已经接上。
- 已经确认：`U0` 当前已不仅是“四大容器能看见”，而是已经包含首轮单容器结构细化与最小运行时界面参数调节。
- 当前仍未完成：真实 `LibraryPanel / ScanPanel` 数据接回、`busy` 状态绑定、导入任务面板真实内容、正式设置持久化。
- 结论是：当前最自然的下一步已经不是继续抽象壳层，而是开始把 `Sidebar` 作为 `U1` 首个真实面板接回点。

## 补充更新：U1 已开始接最小可用的导入/扫描与主界面刷新闭环

### 14. 先把导入动作、扫描动作和主界面三列刷新接成一个最小闭环
在理解旧仓“导入后不跳结果页，而是直接刷新 Sidebar / Main / Metadata”这条链路之后，这一轮开始把新仓的 `U1` 真正落地成最小可用版本。

这件事本身做了什么：先在 `ImportTaskPanel` 里接入本地路径输入、`登记媒体库`、`登记并扫描` 两个动作；再把 `Sidebar` 接成真实媒体库列表、当前扫描摘要和最小扫描动作；随后把 `Main` 接成当前媒体库的最小条目预览，把 `Metadata` 接成当前媒体库、扫描状态和当前选中条目的最小详情。现在导入或扫描成功后，不再只是面板里提示“待接入”，而是会真正触发主界面三列重新读取并刷新。

它的重要性在于：这一步把新仓从“有结构但没有真实数据链”的阶段，推进到了“已经有第一条真实前端操作流”的阶段。虽然还不是旧仓那套完整导入中心，但最核心的动作语义已经立住：入口在大面板，结果回流主工作区。

它对后续的帮助是：接下来继续推进 `U1/U2/U4` 时，不需要再先搭“刷新闭环”这类基础设施，而是可以直接围绕真实面板内容和状态细化继续往前走。

## 补充更新：当前阶段判断再次更新

### 15. 当前已经从“结构细化阶段”进入“U1 首轮接线阶段”
- 已经确认：`U1` 不再是待开始，当前已进入首轮接线状态。
- 已经确认：新仓当前采用的是“导入/扫描动作 -> repository -> 刷新 Sidebar / Main / Metadata”的简化闭环，而不是先做独立导入结果页。
- 当前仍未完成：文件选择器、拖拽、粘贴三入口统一收敛；导入任务 busy 状态；更完整的扫描进度轮询；更真实的 Items / Metadata 数据组织。
- 结论是：下一步应该继续围绕当前已跑通的 `Library / Scan / Items / Metadata` 最小闭环，逐步把占位摘要替换成更真实的面板内容，而不是再回头重做壳层。

## 补充更新：导入入口已补系统文件夹选择器

### 16. 给 ImportTaskPanel 增加系统文件夹选择器，同时保留手动路径输入
在最小导入闭环接通之后，这一轮继续把入口体验往前推了一步。

这件事本身做了什么：为 `ImportTaskPanel` 接入了 Tauri 的系统文件夹选择器，让用户可以直接通过原生目录选择框回填媒体库路径；同时保留现有手动路径输入，作为无法使用系统选择器时的补充入口。宿主侧也同步补上了 dialog plugin 初始化与 capability 权限。

它的重要性在于：这一步把当前 `U1` 从“只能手填路径”的最小实现，推进到“已经具备基础桌面导入入口形态”的阶段。

它对后续的帮助是：后面如果继续补拖拽、粘贴或多入口统一收敛时，当前导入面板已经有了一个更接近桌面应用直觉的原生入口，不需要再回头补最基础的 picker 能力。

## 补充更新：主窗口已开始接全窗口拖拽导入

### 17. 先把拖拽路径直连到当前导入闭环，并在主窗口显示导入提示层
在原生文件夹选择器接通后，这一轮继续补入了导入的第二个自然入口：拖拽。

这件事本身做了什么：利用 Tauri 当前窗口的拖拽事件，在用户把本地路径拖入主窗口时显示一个全窗口提示层；释放后会直接把拖入的路径批量送入当前导入链路，依次执行“登记媒体库 -> 扫描 -> 刷新主界面三列”。如果某些路径失败，当前会在导入面板里回显失败信息，而不是静默丢失。

它的重要性在于：这说明新仓的导入入口已经不再局限于面板内按钮，而是开始具备桌面应用更自然的全窗口导入行为。

它对后续的帮助是：后面如果要继续补粘贴导入、busy 状态、进度轮询或更完整的任务队列，这条拖拽入口已经可以直接复用现有导入闭环，不需要再单独设计另一套结果回流逻辑。

## 补充更新：主窗口已开始接全局粘贴导入

### 18. 先支持从剪贴板文本路径触发导入，并复用当前导入闭环
在拖拽入口接通之后，这一轮继续补入了第三个轻量入口：粘贴。

这件事本身做了什么：在主窗口监听全局 `paste` 事件；如果当前焦点不在输入框、文本域或可编辑区域，并且剪贴板里能解析出本地路径文本，就会直接进入当前导入链路，执行“登记媒体库 -> 扫描 -> 刷新主界面三列”。这一步同时补入了 Tauri 的 clipboard manager 插件，用来在前端 paste 事件拿不到文本时兜底读取系统剪贴板文本。

它的重要性在于：这一步让新仓当前导入入口已经覆盖了“面板选择 / 全窗口拖拽 / 全局粘贴”三种自然桌面交互中的前两种半和第三种轻量版本，开始接近旧仓“多入口统一收敛”的方向。

它对后续的帮助是：后面如果要继续把粘贴从“文本路径导入”扩展到更原生的文件路径读取，或者把导入任务 busy/progress 真正做成统一任务面板，这条 paste 入口已经可以直接复用现有闭环，不需要另起一套刷新逻辑。

## 补充更新：导入/扫描已补入 busy 态与最小进度轮询

### 19. 先把 Logo busy、扫描轮询和进度条接上，让导入面板开始像任务面板
在三类导入入口接通之后，这一轮继续把“任务感”补到当前最小闭环里。

这件事本身做了什么：先让 `Header` 的 Logo 补入 `busy` 状态，当导入动作执行中或扫描任务仍处于 `queued/running` 时，Logo 不再停留在 idle；再对当前选中媒体库的扫描快照做最小轮询，在任务运行期间持续刷新 `scanSnapshot / scanStats`；同时把进度条接入 `Sidebar` 的扫描摘要卡片和 `ImportTaskPanel` 的扫描状态卡片，让用户可以看到任务状态和进度变化。

它的重要性在于：这一步让当前导入大面板不再只是“能触发动作”的入口，而是开始承担“反馈当前任务状态”的职责，逐步向旧仓的导入任务面板语义靠近。

它对后续的帮助是：后面如果继续把当前最小导入闭环扩展成更完整的任务队列、错误重试和多任务面板，这一轮接上的 busy / polling / progress 已经可以作为最小状态骨架继续演进。

## 补充更新：粘贴导入已从文本路径推进到原生文件路径读取优先

### 20. 先读系统剪贴板里的文件列表，再回退到文本路径解析
在全局 `paste` 首轮接通之后，这一轮继续把它补到了更接近桌面应用的行为。

这件事本身做了什么：新增了 `clipboard-x` 插件，用来读取系统剪贴板中的文件列表；当用户在资源管理器里复制文件夹或文件后回到主窗口执行粘贴，当前实现会先尝试读取原生文件路径列表，再在拿不到文件列表时回退到之前已经接上的文本路径解析。无论来源是原生文件列表还是文本路径，最终都会复用现有的导入闭环，不会再分叉成单独逻辑。

它的重要性在于：这一步让“粘贴导入”不再只是一个轻量文本入口，而是开始具备真正的桌面剪贴板文件导入语义，更接近旧仓的实际使用方式。

它对后续的帮助是：后面如果继续补更完整的剪贴板格式支持、任务队列或跨入口统一状态展示，这条粘贴链路已经有了更稳的原生基础，不需要再回退成只吃文本路径的过渡实现。

## 补充更新：数据库管理分页已按 phase 计划完成首轮接线

### 20. 先把 runtime storage、数据库清理和设置分页 UI 一次性闭成最小可用链路
这一轮继续按 `docs/runtime/database-management-phase-plan.md` 的 phase 顺序推进，没有跳过后端能力面。

这件事本身做了什么：先在 `packages/contracts`、桌面端 adapter 和 `MediaRepository` 上补齐 `database.readRuntimeInfo / setStoragePaths / clear`；然后在 Rust 侧新增 `runtime_storage` 模块，把运行时存储配置文件固定到开发态 `data/runtime-storage-paths.json`、打包态 `app local data dir/runtime-storage-paths.json`，并把数据库与缩略图目录的解析顺序切到 `env -> runtime storage config -> default`。在这个基础上，又补了 `read_runtime_info_command`、`set_runtime_storage_paths_command` 和 `clear_database_command`，其中 SQL 目录切换会迁移 `db / -wal / -shm`，清除数据库会同时删除当前 SQL、缩略图缓存、normalize 缓存、playback sessions 和 runtime storage config。

随后在设置面板新增了 `数据库管理` 分页，接上了 `清除数据库`、`选择 SQL 目录`、`选择缩略图目录` 三个动作，并补了路径展示、pending、success、error 和清除后的 `window.location.reload()`。这样现在设置面板已经不再只是界面参数调节，而是开始承担开发期非常需要的运行时重置和路径切换入口。

### 21. 这一轮验证已经完成构建、runtime check 和 Rust 自动化验证
这件事本身做了什么：前端重新执行了 `npm run build:web`；运行时链路执行了 `npm run check`；Rust 宿主执行了 `scripts/run-cargo-with-msvc.cmd test --manifest-path Z:\Playground\CurrentWorking\MediaPlayerNext\src-tauri\Cargo.toml`，新补的 runtime storage 测试也一起通过。为了补主机侧联调，又尝试执行了 `npm run tauri:dev`。

当前结论是：代码构建、运行时检查和 Rust 自动化测试都已经通过，数据库管理相关后端语义也已经被自动化覆盖；但 `tauri:dev` 在本轮被现有 `1420` 端口占用阻塞，所以还没有在真实交互窗口里完成最后一轮手动点验。也就是说，这一批功能已经达到“代码与 CLI 验证完成”的状态，剩下的是进入交互环境后再点一遍设置页上的三个动作。
