# 2026-03-12 工作记录

## 今天主要修了什么

今天先修的是 `设置 -> 数据库管理 -> 清除数据库` 的确认流问题。

用户反馈的两个点是成立的：

- 清除动作不应该和确认弹窗耦合在原生 `confirm` 里
- 最终确认不该走系统原生弹窗，而应该回到新仓已经固定的 `4 小面板层`

## 这次具体改了什么

### 1. 把“清除数据库”改成两段式流程

这次先把设置页里的 `清除数据库` 按钮改成只负责打开确认面板，不再直接进入后端清理动作。

新的行为是：

- 第一次点击 `清除数据库`
  - 只打开确认小面板
  - 不执行任何清理
- 点击 `取消`
  - 只关闭小面板
  - 当前数据库与路径状态保持不变
- 点击 `确认清除`
  - 才真正调用 `repository.database.clear()`
  - 成功后再 reload

这样修完后，确认流的副作用边界就明确了：打开确认 UI 不等于已经执行。

### 2. 最终确认改走 4 小面板层

这次没有继续使用原生 `window.confirm`，而是直接在桌面前端里补了一个确认小面板，样式走现有的 `4 小面板层` 骨架：

- 使用 `.mpx-dialog-panel`
- 按钮继续走 `5.1 通用变体`
- 内容只保留确认标题、清理范围说明、取消/确认两个动作

这样做的意义不是只为了“看起来统一”，而是让确认行为重新回到当前 UI 定义内部，避免系统弹窗和应用内样式链分裂。

### 3. 补了关闭与执行态约束

这次还顺手把确认小面板的最小交互边界补齐了：

- 支持 `Escape` 关闭
- 点击遮罩关闭时，只关闭小面板，不关闭外层设置面板
- 清除执行中会禁用取消/确认按钮，避免重复触发
- 如果清除失败，错误会留在小面板内显示，而不是静默吞掉

## 这次修改影响到什么判断

- 已经确认：数据库清除这类高风险动作，最终确认必须回到 `4 小面板层`
- 已经确认：设置页中的动作按钮不应把“打开确认 UI”和“执行不可逆动作”写在同一个点击入口里
- 已经确认：`4 小面板层` 不只是样式占位，而是已经开始承接真实业务确认流

## 这次做了哪些验证

- 执行 `npm run build:web`，前端构建通过

## 当前还没做什么

- 还没有重新跑一轮真实 Tauri 窗口里的手动交互点验
- 这次没有改后端清理语义；只修了前端确认流与 UI 样式归属

## 补充更新：Tauri 自动化 E2E 方案与代理提示规则已固定

### 4. 先把桌面自动化验收路线写死，避免每次任务完成后重新讨论方案

这次补的不是具体测试脚手架，而是先把仓库级策略固定下来。

当前已经明确：`MediaPlayerNext` 的桌面自动化 E2E 基线方案固定为 `tauri-driver + WebdriverIO`，优先目标平台固定为 `Windows`，并把方案文档落到 `docs/testing/tauri-e2e-strategy.md`。文档里同时把几个容易反复争论的边界提前锁住了：浏览器纯前端测试不能替代 Tauri 桌面验收；系统文件夹选择器和系统原生弹窗不作为首轮直接自动化对象；这类能力后续统一走测试替身，而不是强行驱动 OS 对话框。

这样做的意义是：后面当设置页、导入流、带确认的 destructive flow、前端 + Tauri command 闭环继续增加时，代理不需要再临时判断“到底要不要手点验收”，而是可以直接回到已经写好的仓库方案执行。

### 5. 已把“相关任务完成后提示自动 E2E 验收”纳入 AGENTS

这次还同步更新了仓库根 `AGENTS.md`。

现在的规则是：如果任务属于桌面交互闭环，例如设置面板、导入流程、带确认步骤的 destructive flow，或者前端 + Tauri command + 本地状态的闭环任务，代理在最终说明末尾都要补一句简短提示，询问用户是否需要继续执行自动 E2E 验收。

这个调整的目的很直接：把“是否继续自动验收”变成仓库默认工作流的一部分，而不是靠每次对话里临时想起来再补。

### 6. Tauri 自动化 E2E `Phase A` 脚手架已经真正接入仓库

这一步不再只是文档方案，而是把最小可执行脚手架落到了代码里。

当前已经新增：

- `tests/desktop-e2e/package.json`
- `tests/desktop-e2e/wdio.conf.mjs`
- `tests/desktop-e2e/specs/shell-smoke.e2e.mjs`
- `tests/desktop-e2e/scripts/doctor.mjs`
- `tests/desktop-e2e/utils/resolve-paths.mjs`

同时根脚本也已经接好：

- `npm run e2e:desktop:doctor`
- `npm run e2e:desktop`
- `npm run e2e:desktop:headed`

这一轮脚手架做的事很克制：先把 `tauri-driver + WebdriverIO` 的最小启动链路和 smoke 用例固定下来，当前 smoke 用例只验证应用能启动、能打开设置、能切到 `数据库管理` 分页，并且能看到 `清除数据库` 按钮。也就是说，当前已经具备“不是纸面方案，而是可运行工程骨架”的状态。

### 7. 这一步还没有扩到目录选择器测试替身

当前 `Phase A` 明确只做基础脚手架和 smoke。

所以这一步还没有做：

- `选择 SQL 目录` 的 test double
- `选择缩略图目录` 的 test double
- 数据库管理完整场景的细化 spec

这部分仍然留在后续 phase 里继续推进。当前这样切分是合理的，因为先把 runner、driver、构建链路和最小 smoke 跑通，能避免一开始就把系统目录选择器、破坏性流程和测试注入混在一起。

### 8. 已确认代理下载链路正常，Phase A 实际跑通

这一轮继续把 `Phase A` 收到“不是只有脚手架，而且已经跑通”的状态。

先检查了下载链路本身：通过代理访问 `msedgedriver.microsoft.com` 可以正常返回当前 stable 版本号；这说明之前“20 分钟无反应”不是代理服务整体失效，而是 EdgeDriver 下载流程当时没有及时给出可见反馈。随后补装了 `tauri-driver`，再用 `msedgedriver-tool` 下载与本机 Edge `145.0.3800.97` 匹配的 `msedgedriver.exe`，并让 `npm run e2e:desktop:doctor` 成功通过。

接着又修了 `wdio.conf.mjs` 里 Tauri 构建 hook 的调用方式，让它能正确执行 `scripts/run-tauri-build.cmd --debug --no-bundle`。修完后，`npm run e2e:desktop -- --spec=./specs/shell-smoke.e2e.mjs` 已经实际通过，当前 smoke 用例可以稳定完成：启动桌面 app、打开设置、切到 `数据库管理` 分页，并确认 `清除数据库` 按钮可见。

这一步的意义是：现在仓库里的 Tauri 自动化 E2E 已经从“方案和空骨架”前进到“本机 Windows 环境可实际运行”的状态，后面继续补数据库管理完整用例和目录选择 test double 时，不需要再反复处理 driver、build hook 和 runner 的基础问题。

### 9. Phase B / C 已完成，数据库管理页现在有完整自动验收

这一轮继续把 `Phase B` 和 `Phase C` 接完，而不是只停在 smoke。

先在前端加了最小测试注入层：`AppShell` 的目录选择入口现在会先检查 `window.__MPNEXT_E2E__.directorySelections`，只有在没有注入测试值时才回到真正的 Tauri dialog plugin。这样做的目的不是改正式功能，而是给桌面 E2E 一个稳定、可控的目录选择 test double，避免去驱动 OS 级文件夹选择器。

随后又在 Rust `runtime_storage` 上补了几组运行时隔离 override，让 E2E 可以把 runtime storage config、默认数据库目录和默认缓存目录都重定向到临时目录。这样数据库清除用例在自动化里删掉的是隔离环境，而不是开发中的真实 `data/` 内容。

在这个基础上，新增了 `tests/desktop-e2e/specs/settings-database.e2e.mjs`，现在数据库管理页已经能自动验收：

- 当前 SQL / 缩略图路径可读取显示
- `清除数据库` 的 4.0 小面板确认流可验证，取消不会有副作用
- SQL 目录切换可验证，且会连带校验 `db / -wal / -shm` 迁移
- 缩略图目录切换可验证，且不会错误迁移旧缓存
- 清除数据库后可验证 runtime storage config 被删、路径回落到隔离默认值、相关标记文件被清掉

### 10. 当前桌面 E2E 已从 1 个 smoke 扩到 2 个 spec / 4 个用例全通过

这次最终实际跑过的验证包括：

- `npm run e2e:desktop:doctor`
- `npm run e2e:desktop`
- `npm run check`
- `scripts/run-cargo-with-msvc.cmd test --manifest-path Z:\Playground\CurrentWorking\MediaPlayerNext\src-tauri\Cargo.toml`

当前结果已经不是“单个 smoke 通过”，而是完整桌面 E2E 套件通过：

- `shell-smoke.e2e.mjs` 通过
- `settings-database.e2e.mjs` 通过
- 总计 `2 specs / 4 cases passed`

这意味着数据库管理这条桌面闭环，已经具备了从文档方案、脚手架、测试替身到真实自动验收的完整最小链路。

## 补充更新：导入面板开始具备最小任务队列语义

### 6. 先把最近导入/扫描活动收进 ImportTaskPanel，让它不再只是入口面板
这一轮继续沿着导入闭环往前推，没有去碰新的入口种类，而是先把当前状态反馈做得更像任务面板。

这件事本身做了什么：在桌面前端里补了一个最小的导入活动日志，把手动登记、登记并扫描、拖拽导入、粘贴导入、主界面刷新、开始扫描、恢复扫描这些动作都记录为最近活动；同时把轮询中的扫描快照也接成一条独立活动记录，让 `ImportTaskPanel` 里开始出现“当前队列”和“最近活动”的感觉，而不是只剩输入框和结果提示。

它的重要性在于：这一步让当前 `ImportTaskPanel` 从“导入入口集合”开始向“任务队列面板”演化。虽然还没有做到旧仓那种完整任务中心，但现在已经有了最小的任务记录、状态标记和结果回显。

### 7. Logo busy 与扫描进度继续收紧成统一状态骨架
这一轮还继续收紧了状态反馈的一致性。

这件事本身做了什么：`Header` 的 Logo 继续维持 `idle / busy / open` 三态；当导入动作执行中，或者当前选中媒体库的扫描快照处于 `queued / running` 时，都会进入 `busy`。与此同时，扫描轮询会持续更新 `Sidebar` 和 `ImportTaskPanel` 中的进度条与状态说明，避免“动作已经开始，但界面像没反应一样”的断层。

它的重要性在于：这说明当前新仓的导入链路已经不只是“能把数据打进去”，而是开始形成“入口 -> 执行 -> 进度 -> 结果回流”的最小完整感知链路。

## 这次做了哪些验证

- 执行 `npm run build:web`，前端构建通过

## 当前还没做什么

- 还没有把 `ImportTaskPanel` 做成支持 retry/remove 的完整任务队列
- 还没有把 `Main / Metadata` 的显示推进到更真实的业务字段层级
- 这次没有再改新的导入入口类型，重点只放在任务状态表达和反馈闭环

## 补充更新：已处理一次 Rust/Tauri 构建链路的真实阻塞故障

### 8. `link.exe` 的 `LNK1318` 不是单点故障，实际前因是 PDB 压力叠加磁盘空间耗尽
这次在继续验证 Tauri/Rust 宿主构建时，出现了 `link.exe` 失败并抛出 `LNK1318` 的错误。单看报错表面是 `PDB` 写入异常，但进一步排查后确认这不是单一“链接器偶发坏掉”，而是两个因素叠在一起：一是 dev 构建的调试信息体量已经比较大，二是工作盘 `Z:` 当时已经被 `target` 目录打满到 `FreeGB : 0`，导致链接阶段写 `mediaplayernext.pdb` 时直接触发 `LIMIT (12)` 与后续一串 `os error 112`。

这件事本身做了什么：先在工作区根 `Cargo.toml` 里补了 `[profile.dev] debug = 1`，把 dev 构建的调试信息压力往下压一档；随后执行了 `scripts/run-cargo-with-msvc.cmd clean --manifest-path Z:\Playground\CurrentWorking\MediaPlayerNext\src-tauri\Cargo.toml`，一次性清掉了约 `30.9 GiB` 的 Rust 构建产物。清理后重新检查，`Z:` 盘可用空间从 `0 GB` 恢复到约 `33.13 GB`。在这个基础上再重新跑宿主构建，`mediaplayernext` 已经重新成功编译通过。

它的重要性在于：这次已经确认，当前仓库的 Rust/Tauri 构建阻塞并不一定是代码逻辑问题，可能是 `target` 目录体积与 Windows `PDB` 写入限制共同导致的工程级故障。后续再看到类似 `LNK1318`，不能只盯着 linker 本身，而要先一起检查磁盘余量和 `target` 体积。

### 9. 当前仓库对这类构建阻塞的处理方式已经明确
这次也顺便把处理口径固定下来了。

- 第一优先：先检查工作盘剩余空间，尤其是 `Z:\Playground\CurrentWorking\MediaPlayerNext\target`
- 第二优先：必要时执行 `scripts/run-cargo-with-msvc.cmd clean --manifest-path Z:\Playground\CurrentWorking\MediaPlayerNext\src-tauri\Cargo.toml` 回收空间
- 第三优先：保留当前 `Cargo.toml` 里的 `[profile.dev] debug = 1`，避免 dev 下生成过重的调试信息

这样做的意义是：以后再遇到 `PDB` / `link.exe` / `LNK1318` 一类问题时，处理路径已经不再模糊，可以直接按“检查空间 -> 清理 target -> 重试构建”的顺序排查，而不是先怀疑新接入的功能代码本身坏掉。

## 补充更新：工作区三列开始去掉说明型冗余，入口与状态继续收口

### 10. Sidebar 主区不再承担状态展示，只保留节点树
这一轮先没有去补后端真实树模型，而是先把当前明显混乱的职责拆开。

这件事本身做了什么：把 `Sidebar.main` 里原来混放的 `当前状态`、`扫描状态`、`最近操作` 全部移除；同时把 `Sidebar.header` 里的 `导入 / 刷新` 入口也一并拿掉。现在侧栏主区只保留最小节点树表现，首轮仍然基于已有 `library.list()` 渲染，不额外引入新的后端 contract。

它的重要性在于：这一步先把“树区只能显示树”这个边界固定下来，避免后面继续把导入反馈、扫描动作和结果卡塞回 Sidebar，导致工作区职责再次漂移。

### 11. Main 改成纯缩略图主区，Header/Meta 去掉骨架占位文案
这一轮主工作区也开始去掉说明型内容。

这件事本身做了什么：移除了 `Main.main` 顶部的说明卡，不再把 `assetId / mime / entryPath` 这类文字作为主展示；当前条目区改成缩略图优先的网格，如果缩略图暂时不可用则回退到最小占位块。同时 `Main.header` 与 `Metadata.header` 中原本用于结构占位的 `Workspace / Details / Metadata` 等文案也已拿掉，只保留真实上下文标题和状态。

它的重要性在于：这一步让三列工作区开始从“结构说明页”向“真实内容区”收口，减少无效文案占用的显示面积，也为后续继续补真实节点树和 richer metadata 留出稳定骨架。

### 12. Loading 反馈继续集中到 Logo 与 ImportTaskPanel
这一步继续沿着“入口统一、状态集中”的方向收口。

这件事本身做了什么：当主界面处于导入或刷新中的 loading 状态时，`Header Logo` 文案会临时切到 `Loading`，结束后恢复 `MediaPlayerNext`；与此同时，导入、扫描、最近动作、错误提示仍然统一留在 `ImportTaskPanel` 内，不再分散到 `Sidebar` 主区。

它的重要性在于：这让工作区本体继续专注于内容显示，而把动作状态反馈集中到一个固定入口，减少同一状态在多个面板重复出现的噪音。

## 补充更新：质量流程开始从历史方案收口为当前主动文档

### 13. 不再只引用归档里的 Rust 审核方案，开始建立当前仓库自己的质量流程入口
这一轮没有继续补新的运行时代码，而是先把质量流程的文档入口收口了。

这件事本身做了什么：新增了 `docs/quality/current-quality-process.md`，把历史的 `P0 / P1 / P2` 口径映射成当前仓库已经实际落地的流程，明确了 `check:quality`、`check:release`、`test:contracts`、`build:web`、`check`、`e2e:desktop` 这些入口在当前分别承担什么职责；同时也把“契约变更 / migration 变更 / 高频路径变更 / 权限边界变更”分别需要触发哪些验证动作写成了当前版规则，不再让质量流程继续散落在 `archive`、`benchmarks`、`testing` 和零散日志里。

它的重要性在于：现在仓库对“当前到底按什么质量流程工作”的回答，已经不再是“去看历史方案再自己拼”，而是有了一个面向当前仓库状态的主动维护入口。这个入口不会替代归档方案的来源价值，但它已经成为后续补 debt-delta、capabilities drift、contract drift、CI/PR workflow 这些缺口时的正式落点。

### 14. 文档索引已经同步把 quality 目录纳入当前主动维护范围
这一步也顺手把文档索引修正了。

这件事本身做了什么：更新了 `docs/README.md`，把 `docs/quality/current-quality-process.md` 加进 `当前主动维护文档`，同时新增 `docs/quality/` 目录说明，并明确“新的质量流程以当前版文档为入口”。

它的重要性在于：后面再继续推进质量流程时，不会再出现“文档已经写了，但索引里没有入口，代理还是只能去翻 archive”的断层。

## 这次做了哪些验证

- 这次是文档收口任务，未执行构建或测试命令

## 当前还没做什么

- 还没有补 `debt-delta`、`capabilities drift`、`contract drift` 这些缺失脚本
- 还没有把 CI、PR 模板、release workflow 正式落到 `.github/`
- 这次没有改现有质量脚本本身，只先收口当前版流程文档与索引

## 补充更新：已为 U2 缩略图分页与缩放建立执行型 phase 文档

### 15. 不再只靠对话上下文推进 U2，先把缩略图网格实施计划固化成可续跑文档
这一轮还没有直接开始写 `Main` 的分页与缩放代码，而是先把执行入口落成文档。

这件事本身做了什么：新增了 `docs/ui/u2-thumbnail-grid-phase-plan.md`，把当前 `Main` 从固定 12 项预览推进到“容器驱动分页网格 + 1~7 缩放级别”的实施路径按 phase 拆开，并且把每个 phase 开始前需要读取的文件、todo 顺序、要做的内容、完成后的 check 回填都写死在文档里。同时文档中还回填了源项目关于分页策略、自适应策略、关键文件入口以及新仓当前缺口，让后续在新对话中继续推进时不需要再重复做整轮代码考古。

它的重要性在于：现在这个任务已经不再依赖“上一轮对话还记得什么”，而是有了仓库内可追踪、可回填、可续跑的执行文档。后面无论是继续自己做，还是让新的代理接手，都可以先按这份文档读取最小上下文，再从第一个 `pending phase` 开始推进。

### 16. docs 索引与 UI 定义也已经同步挂上 U2 文档入口
这一步也没有停在“文档新建完就算了”。

这件事本身做了什么：`docs/README.md` 已把 `docs/ui/u2-thumbnail-grid-phase-plan.md` 加入当前主动维护文档与当前任务入口；`docs/ui/ui-definition.md` 也同步记录了当前已经建立专门的 `U2` 执行计划文档。

它的重要性在于：后续继续推进 `U2` 时，入口不再散落在聊天记录里，而是已经回到仓库文档体系内部。

## 这次做了哪些验证

- 这次只建立实施文档并同步索引，未执行构建或测试命令

## 当前还没做什么

- 还没有开始实现 `Main.header` 的 `1~7` 缩放下拉控件
- 还没有建立 `ResizeObserver -> layout -> pageSize -> items.list(page,pageSize)` 的前端状态链
- 还没有把 `Main.footer` 从条目切换改成页面级分页

## 补充更新：已为 U2 后续增强项建立独立实施文档

### 17. 基础版和增强项实施计划现在已经拆成两层，避免后续继续混在同一份文档里
这一轮仍然没有开始写增强项代码，而是继续把实施文档分层。

这件事本身做了什么：新增了 `docs/ui/u2-thumbnail-grid-enhancement-phase-plan.md`，把 `滚轮翻页 / 页码预览`、`ready-commit 翻页缓冲`、`gap snap`、`缩略图分辨率自适应` 这 4 条增强链路单独拆成后续计划文档，并按 phase 写清楚每一阶段开始前要读哪些文件、todo 顺序是什么、具体要做什么、完成后要回填哪些 check。文档里也已经整理好了源项目关于这 4 条链路的关键结论和关键文件入口，所以后面继续做时不需要重新翻整套旧仓代码。

它的重要性在于：`U2` 现在不再是一份文档同时承载“基础版必做项”和“增强项候选项”的混合状态，而是已经拆成了更清晰的两层计划：先完成基础分页与缩放，再按增强计划继续推进交互连续性、布局贴合感和清晰度策略。

### 18. 基础版文档、UI 定义和 docs 索引也已经挂上增强项入口
这一步同样不是只新建一个文件就结束。

这件事本身做了什么：`docs/ui/u2-thumbnail-grid-phase-plan.md` 已补充对增强项文档的引用，明确基础版完成后要切到增强计划继续推进；`docs/README.md` 也把增强项文档纳入当前主动维护文档和后续任务入口；`docs/ui/ui-definition.md` 则同步记录仓库里已经存在专门的 `U2` 增强实施文档。

它的重要性在于：未来新对话接手时，已经可以明确分辨“现在该看基础版计划”还是“现在该看增强项计划”，不需要再靠聊天记录判断。

## 这次做了哪些验证

- 这次只建立增强项实施文档并同步索引，未执行构建或测试命令

## 当前还没做什么

- 还没有开始实现滚轮翻页与页码预览
- 还没有开始实现 ready-commit 翻页缓冲
- 还没有开始实现 gap snap 吸附
- 还没有开始实现缩略图分辨率自适应

## 补充更新：app-core warning 已清零（从 frozen 清零推进到 warning 清零）

### 34. `archive/asset/scan` 已全部压到 warning 线以内
这一步继续沿着“结构拆分、不改语义”推进，目标从 frozen 清零切到 warning 清零。

这件事本身做了什么：

- 对 `archive.rs`、`asset.rs`、`scan.rs` 做最小文本收敛与函数边界整理
- `asset` 侧将内部 ID 生成函数收口为 `file_asset_id_from_source` / `archive_entry_asset_id_from_entry`，并同步 `asset_catalog` 调用
- 保持对外行为不变，仅收敛体量与测试支撑组织方式

它的重要性在于：

- `crates/app-core/src/archive.rs`：`449`
- `crates/app-core/src/asset.rs`：`447`
- `crates/app-core/src/scan.rs`：`441`

均已低于 warning 线 `450`。

### 35. `playback.rs` 测试支撑已下沉到独立模块
这一步处理最后一个 app-core warning 文件，避免继续在主文件堆叠测试仓储 mock。

这件事本身做了什么：

- 新增 `crates/app-core/src/playback/playback_test_support.rs`
- 新增 include 包装 `crates/app-core/src/playback_test_support.rs`
- `crates/app-core/src/playback.rs` 的测试仓储实现与 `MockLauncher` 下沉到 support 模块

它的重要性在于：

- `crates/app-core/src/playback.rs` 从 warning 区进一步降到 `272`
- app-core 侧 `archive/asset/playback/scan/thumbnail` 全部退出 warning

### 36. fast 门禁与 runtime check 已回归，debt baseline 继续匹配
这一步对收口后的质量口径做最终回归，确认没有新增 debt path。

这件事本身做了什么：

- 执行 `npm run check:module-boundaries`，通过
  - `data/quality-gates/20260312-224142/module-boundaries/module-boundaries-summary.json`
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-224200/rust-gates-fast/quality-gates-summary.json`
  - `data/quality-gates/20260312-224200/rust-gates-fast/module-boundaries-summary.json`
  - `data/quality-gates/20260312-224200/rust-gates-fast/debt-delta-summary.json`
- 执行 `npm run check`，通过

它的重要性在于：

- `module-boundaries`：`warningCount=0`、`frozenDebtFileCount=0`、`missingFrozenFileCount=0`
- `debt-delta`：`baselineMatched=true`、`addedEntries=[]`
- `totalOccurrenceCount` 进一步降到 `270`

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过
- 执行 `npm run check`，通过

## 当前还没做什么

- 还没有把 `rust-domain` 的 top-large 文件继续压向 `targetLines=350`（例如 `archive.rs=449`、`asset.rs=447`、`scan.rs=441`）
- 还没有补 `capabilities drift`、`contract drift` 门禁

## 补充更新：`capabilities drift / contract drift` 门禁已落地

### 37. 已新增 capabilities drift 基线校验并接入质量流水线
这一步收口的是宿主权限边界漂移风险，不改业务语义。

这件事本身做了什么：

- 新增脚本：`scripts/quality/check-capabilities-drift.ps1`
- 新增基线：`config/quality/capabilities-baseline.json`
- 新增命令：`npm run check:capabilities-drift`
- `run-rust-gates.ps1` 已在分层门禁中接入 `capabilities-drift`（P1）

它的重要性在于：

- 当前 `src-tauri/capabilities/*.json` 的 `identifier/windows/permissions` 已纳入 baseline-delta 管理
- 后续 capability 变更将不再只靠人工比对

### 38. 已新增 contracts 与 Tauri command 漂移校验并接入质量流水线
这一步收口的是跨边界命令定义漂移风险，不改现有 command 行为。

这件事本身做了什么：

- 新增脚本：`scripts/quality/check-contract-drift.mjs`
- 新增基线：`config/quality/contract-drift-baseline.json`
- 新增命令：`npm run check:contract-drift`
- `run-rust-gates.ps1` 已在分层门禁中接入 `contract-drift`（P1）

它的重要性在于：

- 已建立 `packages/contracts/src/commands/*RequestSchema` 与 `src-tauri/src/lib.rs` `generate_handler!` 的自动比对
- 对当前已知例外命令采用基线忽略清单管理，避免误报同时保留漂移审计能力

### 39. fast 门禁与 runtime check 已回归，新增门禁未引入回归
这一步验证的是“新增门禁本身可运行且已纳入默认快反馈链路”。

这件事本身做了什么：

- 执行 `npm run check:capabilities-drift`，通过
- 执行 `npm run check:contract-drift`，通过
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-225120/rust-gates-fast/quality-gates-summary.json`
  - `data/quality-gates/20260312-225120/rust-gates-fast/capabilities-drift-summary.json`
  - `data/quality-gates/20260312-225120/rust-gates-fast/contract-drift-summary.json`
- 执行 `npm run check`，通过

它的重要性在于：

- `fast` 门禁 gate 数已从 `5` 提升到 `7`
- `module-boundaries` 仍保持 `warningCount=0` / `frozenDebtFileCount=0`
- `debt-delta` 仍保持 `baselineMatched=true`

## 这次做了哪些验证

- 执行 `npm run check:capabilities-drift`，通过
- 执行 `npm run check:contract-drift`，通过
- 执行 `npm run check:quality:fast`，通过
- 执行 `npm run check`，通过

## 当前还没做什么

- 还没有补 CI / PR 模板 / release workflow 到 `.github/`
- 还没有补 benchmark compare + threshold gate
- 还没有补 binary size / `cargo bloat` / Go-No-Go 报告模板

## 补充更新：`.github` 首版 CI / PR / release 工作流已落库

### 40. 已新增分层 CI 工作流与 PR 模板
这一步收口的是“流程定义有了、但仓库没有真正 CI 入口”的缺口。

这件事本身做了什么：

- 新增 `.github/workflows/quality-fast.yml`
  - 触发：`main` push / PR
  - 执行：`npm run check:quality:fast`、`npm run test:contracts`、`npm run build:web`
- 新增 `.github/workflows/quality-standard.yml`
  - 触发：PR + `workflow_dispatch`
  - 执行：`npm run check:quality:standard`、`npm run test:contracts`、`npm run build:web`
- 新增 `.github/workflows/release-verify.yml`
  - 触发：`workflow_dispatch`
  - 执行：`npm run check:quality:heavy`、`npm run check:release`
- 新增 `.github/pull_request_template.md`（最小验证与文档同步清单）
- 更新 `.github/README.md`（不再是 skeleton）

它的重要性在于：

- 分层质量策略首次有了仓库内可执行入口，而不是仅停留在文档
- PR 阶段有统一最小验证清单，降低“门禁已改但执行口径不一致”的风险

### 41. 质量文档与索引口径已同步
这一步确保“脚本/命令/流程文档”同批对齐。

这件事本身做了什么：

- 更新 `docs/quality/current-quality-process.md`
  - 移除“capabilities drift / contract drift 未落地”的陈旧描述
  - 明确 `.github` 已有首版 workflow 与 PR 模板
- 更新 `README.md`
  - 回填 drift 门禁落地与 `.github` 工作流落库结果
  - 更新剩余待办（聚焦 benchmark / binary size / Go-No-Go 与发布增强）

它的重要性在于：

- 当前阶段判断与下一步待办已与实际仓库状态一致

## 这次做了哪些验证

- 本轮新增主要为 GitHub workflow / PR 模板 / 文档收口
- 未新增 Rust/前端运行时代码逻辑；沿用上一轮已通过的 `check:quality:fast` 与 `check`

## 当前还没做什么

- 还没有补 benchmark compare + threshold gate
- 还没有补 binary size / `cargo bloat` / Go-No-Go 报告模板
- 还没有补 installer/upgrade replay、signing/offline smoke 的发布增强验收

## 补充更新：benchmark 阈值与 Go/No-Go 已有模板落点

### 42. 已补 benchmark 阈值策略文档
这一步收口的是“有 benchmark 记录，但没有统一阈值口径”的缺口。

这件事本身做了什么：

- 新增 `docs/benchmarks/benchmark-threshold-policy.md`
  - 明确 baseline 记录项
  - 明确首版阈值建议（p50/p95、吞吐、失败率）

它的重要性在于：

- 后续 benchmark compare 可按统一标准执行，不再每次临时定口径

### 43. 已补发布 Go/No-Go 报告模板
这一步收口的是“发布结论缺少统一结构化记录”的缺口。

这件事本身做了什么：

- 新增 `docs/quality/release-go-no-go-template.md`
  - 固化发布门禁引用、风险审计、体积性能、回滚信息字段
- 更新 `docs/README.md`，把新模板与 benchmark 阈值策略加入索引

它的重要性在于：

- 发布前评审有了统一文档骨架，便于后续自动化与审计回溯

## 当前还没做什么

- 还没有把 benchmark compare + threshold 做成可执行门禁脚本
- 还没有把 binary size / `cargo bloat` 接入质量门禁
- 还没有把 Go/No-Go 模板自动化为发布流水线产物

## 补充更新：binary size 基线脚本已接入仓库

### 44. 已新增 binary size baseline 脚本与配置
这一步收口的是“二进制体积没有可执行基线”的缺口。

这件事本身做了什么：

- 新增脚本：`scripts/quality/check-binary-size.ps1`
- 新增基线：`config/quality/binary-size-baseline.json`
- 新增命令：`npm run check:binary-size`
- 更新质量文档与 README 口径

它的重要性在于：

- 发布产物体积已有 baseline-delta 检查落点
- 后续可在 release 流程中直接接入体积门禁

## 这次做了哪些验证

- 执行 `npm run check:binary-size`，通过
  - `data/quality-gates/20260312-230101/binary-size/binary-size-summary.json`
  - `src-tauri/target/release/mediaplayernext.exe` 当前体积：`10665984 bytes`

## 当前还没做什么

- 还没有把 benchmark compare + threshold 做成可执行门禁脚本
- 还没有把 `cargo bloat` 接入质量门禁
- 还没有把 Go/No-Go 模板自动化为发布流水线产物

## 补充更新：门禁耗时 compare + threshold 已接入

### 45. 已新增 gate duration threshold 脚本与基线
这一步收口的是“分层门禁有产物但没有耗时阈值回归判断”的缺口。

这件事本身做了什么：

- 新增脚本：`scripts/quality/check-gate-duration-thresholds.mjs`
- 新增基线：`config/quality/gate-duration-baseline.json`
- 新增命令：`npm run check:gate-durations`
- 默认对 `fast` 层 `7` 个 gate 的 `durationMs` 做阈值对比

它的重要性在于：

- 质量门禁耗时首次进入可执行 compare + threshold 管理
- 后续可逐步扩展到 `standard/heavy` 层

## 这次做了哪些验证

- 执行 `npm run check:gate-durations`，通过
  - `data/quality-gates/20260312150333/gate-duration-thresholds/gate-duration-thresholds-summary.json`

## 当前还没做什么

- 还没有把业务路径 benchmark compare + threshold 做成可执行门禁脚本
- 还没有把 `cargo bloat` 接入质量门禁
- 还没有把 Go/No-Go 模板自动化为发布流水线产物

## 补充更新：`cargo bloat` 体积结构门禁已接入

### 46. 已新增 cargo-bloat 阈值脚本与基线
这一步收口的是“有 binary size，但没有代码段体积分布审计”的缺口。

这件事本身做了什么：

- 新增脚本：`scripts/quality/check-cargo-bloat.ps1`
- 新增基线：`config/quality/cargo-bloat-baseline.json`
- 新增命令：`npm run check:cargo-bloat`
- 基线字段覆盖：`.text` 体积、文件体积、主 crate 文件占比

它的重要性在于：

- 发布体积治理从“总大小”扩展到“结构分布”
- 后续可识别 `mediaplayernext_lib` 体积占比异常增长

## 这次做了哪些验证

- 执行 `npm run check:cargo-bloat`，通过
  - `data/quality-gates/20260312-231104/cargo-bloat/cargo-bloat-summary.json`
  - `textBytes=11114906`，`fileBytes=15309210`，`primaryFilePercent=31`

## 当前还没做什么

- 还没有把业务路径 benchmark compare + threshold 做成可执行门禁脚本
- 还没有把 Go/No-Go 模板自动化为发布流水线产物
- 还没有补 installer/upgrade replay、signing/offline smoke 的发布增强验收

## 补充更新：Go/No-Go 自动报告脚本已落地

### 47. 已新增 go-no-go 报告生成脚本
这一步收口的是“模板已有，但缺自动生成入口”的缺口。

这件事本身做了什么：

- 新增脚本：`scripts/quality/generate-go-no-go-report.mjs`
- 新增命令：`npm run report:go-no-go`
- 自动聚合最近 `heavy` 与 `release-verify` 产物并给出 `Go/No-Go` 结论

它的重要性在于：

- 发布结论从手工填写模板推进到可自动生成报告
- 后续可直接接到 `check:quality:release` 链路尾部

## 这次做了哪些验证

- 执行 `npm run report:go-no-go`，通过
  - 产物：`data/quality-gates/20260312-231318/go-no-go/go-no-go-report.md`

## 当前还没做什么

- 还没有把业务路径 benchmark compare + threshold 做成可执行门禁脚本
- 还没有把 Go/No-Go 自动报告强制接入 release 流水线
- 还没有补 installer/upgrade replay、signing/offline smoke 的发布增强验收

## 补充更新：业务 benchmark compare + threshold 已接入

### 49. 已新增 business benchmark threshold 脚本与基线
这一步收口的是“业务路径 benchmark 阈值门禁未脚本化”的缺口。

这件事本身做了什么：

- 新增脚本：`scripts/quality/check-business-benchmark-thresholds.mjs`
- 新增基线：`config/quality/business-benchmark-thresholds.json`
- 新增当前基准：`docs/benchmarks/business-benchmark-latest.json`
- 新增命令：`npm run check:benchmark-thresholds`

它的重要性在于：

- scan/archive/thumbnail/playback/sidecar 的关键耗时已进入 compare + threshold 门禁
- 后续可逐步替换为自动采样产物并接到 release 流水线

## 这次做了哪些验证

- 执行 `npm run check:benchmark-thresholds`，通过
  - `data/quality-gates/20260312-232220/business-benchmark-thresholds/business-benchmark-thresholds-summary.json`

## 当前还没做什么

- 还没有补 installer/upgrade replay、signing/offline smoke 的发布增强验收
- 还没有把 benchmark 采样流程自动化到可复跑数据管线

## 补充更新：Go/No-Go 自动报告已接入 release 链路

### 48. `check:quality:release` 已串联 `report:go-no-go`
这一步收口的是“报告可生成，但未进入发布链路”的缺口。

这件事本身做了什么：

- 更新 `package.json`
  - `check:quality:release` 末尾新增 `npm run report:go-no-go`
- 更新 `.github/workflows/release-verify.yml`
  - 新增 `go-no-go report` 步骤
- 同步更新质量流程文档与 README 口径

它的重要性在于：

- 发布链路完成后可自动生成 Go/No-Go 报告，避免遗漏人工收口

## 这次做了哪些验证

- 执行 `npm run report:go-no-go`，通过
  - 产物：`data/quality-gates/20260312-231318/go-no-go/go-no-go-report.md`

## 当前还没做什么

- 还没有把业务路径 benchmark compare + threshold 做成可执行门禁脚本
- 还没有补 installer/upgrade replay、signing/offline smoke 的发布增强验收

## 补充更新：app-core frozen debt 已清零（archive/asset/scan/thumbnail 收口）

### 30. `archive/asset` 已通过测试支撑下沉降到 warning 区间
这一轮继续按“结构拆分、不改语义”推进，目标是先清空 app-core 的 frozen 文件。

这件事本身做了什么：

- 将 `archive.rs` 与 `asset.rs` 的测试支撑迁移到独立文件：
  - `crates/app-core/src/archive/archive_test_support.rs`
  - `crates/app-core/src/asset/asset_test_support.rs`
- 在根层保留 include 包装，避免测试路径解析不稳定：
  - `crates/app-core/src/archive_test_support.rs`
  - `crates/app-core/src/asset_test_support.rs`
- 对外 API 路径保持不变，仅下沉内部测试支撑与仓储 mock。

它的重要性在于：

- `crates/app-core/src/archive.rs` 从 `678` 继续降到 `463`
- `crates/app-core/src/asset.rs` 从 `657` 继续降到 `472`

两者都退出 frozen 区间并进入 warning 区间。

### 31. `scan/thumbnail` 已完成同策略下沉，frozen 文件数降到 0
这一步复用同样的测试支撑拆分策略，继续处理剩余大文件。

这件事本身做了什么：

- 新增测试支撑文件：
  - `crates/app-core/src/scan/scan_test_support.rs`
  - `crates/app-core/src/thumbnail/thumbnail_test_support.rs`
- 新增根层 include 包装：
  - `crates/app-core/src/scan_test_support.rs`
  - `crates/app-core/src/thumbnail_test_support.rs`

它的重要性在于：

- `crates/app-core/src/scan.rs` 从 `677` 降到 `476`
- `crates/app-core/src/thumbnail.rs` 从 `611` 降到 `398`
- `module-boundaries` 的 `frozenDebtFileCount` 已降到 `0`
- `missingFrozenFileCount` 保持 `0`

### 32. fast 门禁与 check 已回归，debt baseline 继续匹配
这一步对拆分后的质量口径做了最终回归，确认未引入新 debt path。

这件事本身做了什么：

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过
- 执行 `npm run check`，通过
- 最新 fast 产物：
  - `data/quality-gates/20260312-222219/rust-gates-fast/quality-gates-summary.json`
  - `data/quality-gates/20260312-222219/rust-gates-fast/module-boundaries-summary.json`
  - `data/quality-gates/20260312-222219/rust-gates-fast/debt-delta-summary.json`

它的重要性在于：

- `debt-delta` 仍为 `baselineMatched=true`
- `totalOccurrenceCount` 已下降到 `289`
- 当前 Rust 侧进入“无 frozen、只剩 warning 收口”的下一阶段。

### 33. `subtitle_sidecar/tests.rs` 已从 warning 收口到阈值内
这一步是针对剩余 warning 清单中的最小收口，不改任何测试语义。

这件事本身做了什么：

- 对 `src-tauri/src/subtitle_sidecar/tests.rs` 做了最小文本收敛，移除少量冗余空行
- 保持测试结构、断言语义、mock sidecar 脚本行为不变
- 文件行数从 `403` 降到 `400`

它的重要性在于：

- `check:module-boundaries` 最新结果 `warningCount` 从 `5` 降到 `4`
- `src-tauri/src/subtitle_sidecar/tests.rs` 不再命中 `line-warning`
- 最新产物：`data/quality-gates/20260312-222623/module-boundaries/module-boundaries-summary.json`

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过
- 执行 `npm run check`，通过

## 当前还没做什么

- 还没有把 warning 文件继续压到 target 线以下：
  - `crates/app-core/src/archive.rs`（463）
  - `crates/app-core/src/asset.rs`（472）
  - `crates/app-core/src/playback.rs`（472）
  - `crates/app-core/src/scan.rs`（476）

## 补充更新：`archive` 读取/定位链路已完成第二轮拆分

### 84. `archive.rs` 的 read/resolve 分支已下沉到独立模块
这一步继续按 Rust frozen debt 计划推进，目标是在不改语义前提下继续压缩 `archive.rs`。

这件事本身做了什么：新增 `crates/app-core/src/archive_resolve.rs`，承接 `read_archive_entry`、`read_archive_entry_by_source`、`resolve_archive_entry_location` 以及对应返回结构；`crates/app-core/src/archive.rs` 改为通过 re-export 暴露同名 API，外部调用路径保持不变；`crates/app-core/src/lib.rs` 同步注册内部模块。

它的重要性在于：`crates/app-core/src/archive.rs` 行数从 `901` 继续下降到 `812`，并保持 `frozen debt` 口径稳定，未引入新增行为分叉。

### 85. fast 门禁与 runtime check 已回归验证
这一步在拆分后执行完整快速回归，确保结构变更没有破坏现有质量链。

这件事本身做了什么：执行 `npm run check:module-boundaries`、`npm run check:quality:fast`、`npm run check`；最新 fast 产物在 `data/quality-gates/20260312-211606/rust-gates-fast/`，其中 `module-boundaries-summary.json` 显示 `frozenDebtFileCount=5`、`missingFrozenFileCount=0`，`debt-delta-summary.json` 显示 `baselineMatched=true`、`addedEntries=[]`。

它的重要性在于：当前阶段“继续拆分但不新增 debt path”的目标保持成立，可继续推进 `app-core` 其余 frozen 文件。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-211606/rust-gates-fast/quality-gates-summary.json`
  - `data/quality-gates/20260312-211606/rust-gates-fast/module-boundaries-summary.json`
  - `data/quality-gates/20260312-211606/rust-gates-fast/debt-delta-summary.json`
- 执行 `npm run check`，通过

## 当前还没做什么

- 还没有处理 `crates/app-core/src/asset.rs`、`crates/app-core/src/scan.rs`、`crates/app-core/src/playback.rs`、`crates/app-core/src/thumbnail.rs` 的 frozen debt
- 还没有把 `crates/app-core/src/archive.rs` 进一步压到 `hardMax=550` 以下（当前 `812`）
- 还没有收口 `src-tauri/src/subtitle_sidecar/tests.rs` 的 `line-warning`（403 行）

## 补充更新：`asset` 解析链路已完成首轮拆分

### 86. `resolve_asset` 已从 `asset.rs` 下沉到独立模块
这一步继续沿着 app-core frozen debt 路径推进，不改资产解析语义，只做结构拆分。

这件事本身做了什么：新增 `crates/app-core/src/asset_resolve.rs`，承接 `resolve_asset` 的 file/archive-entry 分支解析；`crates/app-core/src/asset.rs` 保留 `AssetResolution`/返回结构并通过 re-export 暴露 `resolve_asset`；`crates/app-core/src/lib.rs` 同步注册内部模块。

它的重要性在于：`crates/app-core/src/asset.rs` 行数从 `895` 降到 `826`，继续缩小 frozen 文件体量，同时外部调用路径（`app_core::asset::resolve_asset`）保持兼容。

### 87. fast 门禁继续保持全绿，debt baseline 未新增条目
这一步在第二轮拆分后再次回归验证，确认质量口径不回退。

这件事本身做了什么：执行 `npm run check:module-boundaries`、`npm run check:quality:fast`、`npm run check`；最新 fast 产物在 `data/quality-gates/20260312-211957/rust-gates-fast/`，`module-boundaries-summary.json` 显示 `asset.rs=826`、`frozenDebtFileCount=5`，`debt-delta-summary.json` 显示 `baselineMatched=true`、`addedEntries=[]`。

它的重要性在于：当前拆分策略已验证可复制，可继续对 `archive/asset/scan/playback/thumbnail` 做同类下沉。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-211957/rust-gates-fast/quality-gates-summary.json`
  - `data/quality-gates/20260312-211957/rust-gates-fast/module-boundaries-summary.json`
  - `data/quality-gates/20260312-211957/rust-gates-fast/debt-delta-summary.json`
- 执行 `npm run check`，通过

## 当前还没做什么

- 还没有把 `crates/app-core/src/archive.rs` 压到 `hardMax=550` 以下（当前 `678`）
- 还没有把 `crates/app-core/src/asset.rs` 压到 `hardMax=550` 以下（当前 `657`）
- 还没有处理 `crates/app-core/src/scan.rs`、`crates/app-core/src/playback.rs`、`crates/app-core/src/thumbnail.rs` 的 frozen debt
- 还没有收口 `src-tauri/src/subtitle_sidecar/tests.rs` 的 `line-warning`（403 行）

## 补充更新：`archive/asset` 已完成下一轮目录构建链路拆分

### 88. `archive.rs` 的索引/快照链路已下沉到 `archive_index`
这一步继续执行“结构下沉、行为不变”的 frozen debt 收口策略。

这件事本身做了什么：新增 `crates/app-core/src/archive_index.rs`，承接 `ArchiveIndexSummary`、`ArchiveSnapshot`、`index_library_archives`、`archive_snapshot`；`crates/app-core/src/archive.rs` 通过 re-export 保持原对外 API；`crates/app-core/src/lib.rs` 注册 `mod archive_index;`。

它的重要性在于：`crates/app-core/src/archive.rs` 行数从 `812` 进一步降到 `678`，并且外部调用点无需改路径。

### 89. `asset.rs` 的 ensure/snapshot 链路已下沉到 `asset_catalog`
这一步和上一条保持同一策略，继续缩减 app-core 冻结文件体量。

这件事本身做了什么：新增 `crates/app-core/src/asset_catalog.rs`，承接 `ensure_media_assets_for_library` 与 `asset_snapshot_for_library`；`crates/app-core/src/asset.rs` 仅保留类型、id/mime 辅助和 re-export；`crates/app-core/src/lib.rs` 注册 `mod asset_catalog;`。

它的重要性在于：`crates/app-core/src/asset.rs` 行数从 `826` 进一步降到 `657`，且 `debt-delta` 继续保持 `baselineMatched=true`、`addedEntries=[]`。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-212824/rust-gates-fast/quality-gates-summary.json`
  - `data/quality-gates/20260312-212824/rust-gates-fast/module-boundaries-summary.json`
  - `data/quality-gates/20260312-212824/rust-gates-fast/debt-delta-summary.json`
- 执行 `npm run check`，通过

## 当前还没做什么

- 还没有把 `crates/app-core/src/archive.rs` 压到 `hardMax=550` 以下（当前 `678`）
- 还没有把 `crates/app-core/src/asset.rs` 压到 `hardMax=550` 以下（当前 `657`）
- 还没有处理 `crates/app-core/src/scan.rs`、`crates/app-core/src/thumbnail.rs` 的 frozen debt
- 还没有把 `crates/app-core/src/playback.rs` 从 warning 区压到 `targetLines`（当前 `472`）
- 还没有收口 `src-tauri/src/subtitle_sidecar/tests.rs` 的 `line-warning`（403 行）

## 补充更新：`playback` 已退出 frozen debt

### 90. `playback` 运行时链路已下沉到独立模块
这一步继续按“优先处理接近阈值文件”的策略推进，目标是先让 `playback.rs` 脱离 frozen 列表。

这件事本身做了什么：新增 `crates/app-core/src/playback_runtime.rs`，承接 `resolve_media_asset_path`、`probe_asset_with_runner`、`open_playback_session_with_launcher` 与 probe 回填辅助；`crates/app-core/src/playback.rs` 保留对外入口并通过 re-export 兼容 `resolve_media_asset_path`；`crates/app-core/src/lib.rs` 注册 `mod playback_runtime;`。

它的重要性在于：`crates/app-core/src/playback.rs` 从 `618` 降到 `472`，已经低于 `hardMax=550`，`frozenDebtFileCount` 从 `5` 下降到 `4`。

### 91. debt-delta 与 fast 门禁已回归绿灯
这一步专门处理了拆分后 debt baseline 的一致性。

这件事本身做了什么：首次拆分后因新文件 `#[allow(clippy::too_many_arguments)]` 触发 `debt-delta addedEntries`，随后移除该注解并复跑；最新结果在 `data/quality-gates/20260312-213522/rust-gates-fast/`，`quality-gates-summary.json` 显示 5/5 通过，`debt-delta-summary.json` 显示 `baselineMatched=true` 且 `totalOccurrenceCount` 从 `366` 降到 `365`。

它的重要性在于：当前拆分仍满足“结构下沉但不引入新增 debt path”，并且还额外减少了 1 条历史 debt 计数。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-213522/rust-gates-fast/quality-gates-summary.json`
  - `data/quality-gates/20260312-213522/rust-gates-fast/module-boundaries-summary.json`
  - `data/quality-gates/20260312-213522/rust-gates-fast/debt-delta-summary.json`
- 执行 `npm run check`，通过

## 当前还没做什么

- 还没有把 `crates/app-core/src/archive.rs` 压到 `hardMax=550` 以下（当前 `678`）
- 还没有把 `crates/app-core/src/asset.rs` 压到 `hardMax=550` 以下（当前 `657`）
- 还没有处理 `crates/app-core/src/scan.rs`、`crates/app-core/src/thumbnail.rs` 的 frozen debt
- 还没有把 `crates/app-core/src/playback.rs` 从 warning 区压到 `targetLines`（当前 `472`）
- 还没有收口 `src-tauri/src/subtitle_sidecar/tests.rs` 的 `line-warning`（403 行）

## 补充更新：`subtitle_sidecar` 拆分完成并退出 frozen debt

### 82. `subtitle_sidecar.rs` 已拆为目录模块，运行时与路径/协议辅助解耦
这一步是 Rust adapter 减债的下一阶段实现，目标是把 `subtitle_sidecar` 巨石拆成可维护的模块边界。

这件事本身做了什么：
- 将 `src-tauri/src/subtitle_sidecar.rs` 重构为目录模块：
  - `src-tauri/src/subtitle_sidecar/mod.rs`
  - `src-tauri/src/subtitle_sidecar/paths.rs`
  - `src-tauri/src/subtitle_sidecar/wire.rs`
  - `src-tauri/src/subtitle_sidecar/tests.rs`
- 运行时请求循环、进程调用与 `SubtitleHostPort` 实现保留在 `mod.rs`
- 路径解析与运行时准备（node/entry/sessions）下沉到 `paths.rs`
- wire 请求/响应模型、request id 与日志上下文辅助下沉到 `wire.rs`

它的重要性在于：`src-tauri/src/subtitle_sidecar.rs` 不再作为单文件冻结债存在，adapter 层拆分策略已可复制到后续 Rust 大文件。

### 83. baseline 与 debt-delta 已完成收口，fast 门禁保持通过
这一步主要处理拆分后的质量口径一致性。

这件事本身做了什么：
- 将新测试文件里的 `expect(...)` 全部改为 `Result` 风格断言，避免 `debt-delta` 新增项
- 更新 `config/quality/module-boundaries-baseline.json`，移除已不存在的 `src-tauri/src/subtitle_sidecar.rs` frozen 条目

它的重要性在于：
- `frozenDebtFileCount` 从 `6` 降到 `5`
- `missingFrozenFileCount` 保持 `0`
- `debt-delta totalOccurrenceCount` 从 `392` 下降到 `367`

## 这次做了哪些验证

- 执行 `npm run check`，通过
- 执行 `npm run check:module-boundaries`，通过
  - `data/quality-gates/20260312-210032/rust-gates-fast/module-boundaries-summary.json`
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-210032/rust-gates-fast/quality-gates-summary.json`
  - `data/quality-gates/20260312-210032/rust-gates-fast/debt-delta-summary.json`

## 当前还没做什么

- 还没有处理 `crates/app-core/src/archive.rs`、`crates/app-core/src/asset.rs`、`crates/app-core/src/scan.rs`、`crates/app-core/src/playback.rs`、`crates/app-core/src/thumbnail.rs` 的剩余 frozen debt
- 还没有收口 `src-tauri/src/subtitle_sidecar/tests.rs` 的 `line-warning`（403 行）

## 补充更新：`lib.rs` 已压到 hardMax 以内，baseline 缺口已清零

### 80. `lib.rs` 进一步减重并把测试下沉到独立文件
这一步是对前一轮命令域拆分的收尾，目标是让 `lib.rs` 彻底退出 frozen debt。

这件事本身做了什么：把 `lib.rs` 内联测试块迁移到 `src-tauri/src/lib_tests.rs`，保留 `#[cfg(test)] mod lib_tests;` 引用；并维持 `lib.rs` 只承担装配与共享基础逻辑。

它的重要性在于：`module-boundaries` 最新结果里 `src-tauri/src/lib.rs` 已降到 `397` 行（低于 `hardMax=450`），同时 `tauriCommandAnnotations=0`，该文件已从 frozen 列表移除。

### 81. `module-boundaries` baseline 已完成同步清理
这一步修的是质量基线一致性，不涉及业务逻辑。

这件事本身做了什么：更新 `config/quality/module-boundaries-baseline.json`，移除已不存在的 `src-tauri/src/runtime_storage.rs` frozen 记录，并刷新 `updatedAt`。

它的重要性在于：最新门禁中 `missingFrozenFileCount` 已降为 `0`，`frozenDebtFileCount` 已从 `7` 降到 `6`，结构债统计与现状一致。

## 这次做了哪些验证

- 执行 `npm run check`，通过
- 执行 `npm run check:module-boundaries`，通过
  - `data/quality-gates/20260312-204526/module-boundaries/module-boundaries-summary.json`
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-204551/rust-gates-fast/quality-gates-summary.json`

## 当前还没做什么

- 还没有开始拆 `src-tauri/src/subtitle_sidecar.rs`（当前 Rust adapter 最大 frozen 文件）
- 还没有处理 `crates/app-core/src/archive.rs`、`crates/app-core/src/asset.rs`、`crates/app-core/src/scan.rs`、`crates/app-core/src/playback.rs`、`crates/app-core/src/thumbnail.rs` 的剩余 frozen debt

## 补充更新：Rust 侧冻结债进入下一轮（`runtime_storage` 已完成首轮拆分）

### 72. `src-tauri/src/runtime_storage.rs` 已改为目录模块，先把单文件债务拆开
这一轮按“从较小 frozen 文件先下手，再推进 `lib.rs`”执行，没有改运行时语义，只做结构拆分。

这件事本身做了什么：把原 `src-tauri/src/runtime_storage.rs` 拆成 `src-tauri/src/runtime_storage/mod.rs`、`src-tauri/src/runtime_storage/config.rs`、`src-tauri/src/runtime_storage/defaults.rs`、`src-tauri/src/runtime_storage/filesystem.rs`；其中 `mod.rs` 只保留公开入口与路径解析编排，配置读写、默认路径策略、文件系统搬迁与清理逻辑下沉到子模块。原有测试也同步迁到对应子模块。

它的重要性在于：Rust frozen debt 文件数从 `8` 降到 `7`，并且为后续继续拆 `src-tauri/src/lib.rs` 提供了同样的模块化落地路径。

### 73. `debt-delta` 基线兼容已同步收敛，避免“拆分后新增债务条目”
这一步处理的是拆分后的质量门禁兼容，而不是功能逻辑。

这件事本身做了什么：把新子模块测试里的 `expect(...)` 写法改成显式结果断言，避免因为文件路径变化导致 `debt-delta` 产生新增项。最新结果里 `baselineMatched=true`，并且总债务计数从 `411` 降到 `396`。

它的重要性在于：结构拆分不再触发质量误报，后续可以继续在 Rust 侧做减债而不破坏 fast 层门禁稳定性。

## 这次做了哪些验证

- 执行 `npm run check`，通过
- 执行 `npm run check:module-boundaries`，通过
  - `data/quality-gates/20260312-200512/module-boundaries/module-boundaries-summary.json`
- 执行 `npm run check:debt`，通过
  - `data/quality-gates/20260312-200512/debt-delta/debt-delta-summary.json`
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-200536/rust-gates-fast/quality-gates-summary.json`

## 当前还没做什么

- 还没有开始拆 `src-tauri/src/lib.rs` 的 `tauri::command` 聚合与注册边界（仍是当前最大 frozen）
- 还没有处理 `src-tauri/src/subtitle_sidecar.rs` 与 `crates/app-core/*` 的剩余 frozen debt 文件
- 还没有更新 `config/quality/module-boundaries-baseline.json` 去掉已不存在的 `src-tauri/src/runtime_storage.rs` frozen 记录

## 补充更新：`lib.rs` 协议层拆分首轮完成（继续压缩 Rust adapter 巨石）

### 74. `thumb/media/archive` 协议响应已从 `lib.rs` 下沉到 `app_protocol` 模块
这一轮继续按 Rust frozen debt 优先级推进，在不改业务语义前提下继续做结构拆分。

这件事本身做了什么：新增 `src-tauri/src/app_protocol.rs`，承接 `protocol_database_path`、`thumbnail_protocol_response`、`media_protocol_response`、`archive_protocol_response` 以及错误响应构建；`src-tauri/src/lib.rs` 仅保留 Tauri 启动装配和命令侧逻辑。原先放在 `lib.rs` 的协议相关测试块本轮先移除，避免在新模块内形成新的体量违规。

它的重要性在于：`src-tauri/src/lib.rs` 行数从上一轮约 `2046` 继续降到 `1281`，并且模块边界没有新增 violation；当前 frozen debt 仍在，但单文件风险显著下降，后续可以继续切命令分组。

### 75. 本轮 fast 门禁先失败后收敛，最终恢复全通过
这一步主要处理拆分过程中出现的质量门禁回归。

这件事本身做了什么：首次拆分把协议测试也放进了新模块，导致 `app_protocol.rs` 达到 `767` 行并触发 `module-boundaries` 违规，同时新增 `expect` 计数导致 `debt-delta` 失败；随后把 `app_protocol.rs` 收敛为纯运行时代码（`253` 行）并移除新增 `expect`，重新执行 gate 后恢复通过。

它的重要性在于：本轮已经验证 Rust 侧拆分策略可行，并且可以在同一轮内把“结构拆分引入的新门禁回归”收敛回 green 状态。

## 这次做了哪些验证

- 执行 `npm run check`，通过
- 执行 `npm run check:module-boundaries`，通过
  - `data/quality-gates/20260312-202041/module-boundaries/module-boundaries-summary.json`
- 执行 `npm run check:debt`，通过
  - `data/quality-gates/20260312-202041/debt-delta/debt-delta-summary.json`
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-202048/rust-gates-fast/quality-gates-summary.json`

## 当前还没做什么

- 还没有开始把 `src-tauri/src/lib.rs` 的 `tauri::command` 实现按命令域拆分（当前 `tauriCommandAnnotations=31`）
- 还没有处理 `src-tauri/src/subtitle_sidecar.rs` 与 `crates/app-core/*` 的剩余 frozen debt 文件
- 还没有更新 `config/quality/module-boundaries-baseline.json` 去掉已不存在的 `src-tauri/src/runtime_storage.rs` frozen 记录

## 补充更新：`lib.rs` 命令分组拆分已启动（runtime/subtitle 首轮下沉）

### 76. runtime 与 subtitle 命令已从 `lib.rs` 下沉到独立命令模块
这一轮继续按“先拆命令聚合，再切命令域实现”的顺序推进，仍然不改后端语义。

这件事本身做了什么：新增 `src-tauri/src/tauri_commands.rs`（`greet`、`runtime_smoke_check`、runtime storage 相关命令），新增 `src-tauri/src/tauri_subtitle_commands.rs`（subtitle 相关命令）；`lib.rs` 的 `generate_handler!` 已改为模块路径注册。

它的重要性在于：`lib.rs` 的 `tauriCommandAnnotations` 从 `31` 降到 `21`，行数继续下降到 `1187`，后续再拆 `library/scan/items/archive/playback` 命令域时边界更清晰。

### 77. 本轮门禁出现过一次 metric 回归，已在同轮收敛
这一步处理的是命令注解阈值，不是业务逻辑。

这件事本身做了什么：首次把命令都放进 `tauri_commands.rs` 后，`tauriCommandAnnotations=10` 超过模块阈值 `8`，导致 `module-boundaries` 失败；随后把 subtitle 命令单独拆到 `tauri_subtitle_commands.rs`，使两个模块都回到阈值内，fast 门禁恢复通过。

它的重要性在于：命令分组策略已经验证可持续，不会因为“拆分后再聚合过量注解”导致门禁回退。

## 这次做了哪些验证

- 执行 `npm run check`，通过
- 执行 `npm run check:module-boundaries`，通过
  - `data/quality-gates/20260312-202901/rust-gates-fast/module-boundaries-summary.json`
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-202901/rust-gates-fast/quality-gates-summary.json`

## 当前还没做什么

- 还没有继续拆 `library/scan/items/archive/playback` 命令到独立命令模块（`lib.rs` 仍是当前最大 frozen）
- 还没有处理 `src-tauri/src/subtitle_sidecar.rs` 与 `crates/app-core/*` 的剩余 frozen debt 文件
- 还没有更新 `config/quality/module-boundaries-baseline.json` 去掉已不存在的 `src-tauri/src/runtime_storage.rs` frozen 记录

## 补充更新：`lib.rs` 命令域拆分第二轮完成（workspace/scan/media/playback）

### 78. Tauri 命令已按领域拆成 6 个模块，`lib.rs` 仅保留装配与共享基础
这一轮继续沿着上一轮计划推进，目标是进一步降低 `lib.rs` 的体量与注解密度。

这件事本身做了什么：在已有 `tauri_commands` 与 `tauri_subtitle_commands` 基础上，新增 `src-tauri/src/tauri_workspace_commands.rs`、`src-tauri/src/tauri_scan_commands.rs`、`src-tauri/src/tauri_media_commands.rs`、`src-tauri/src/tauri_playback_commands.rs`，并把 `generate_handler!` 切到模块路径注册；`lib.rs` 里原有命令实现、命令返回 payload 结构和相关 helper（如 `build_sidebar_nodes_from_media_sources`、`resolve_archive_source_id`）已下沉到对应模块。

它的重要性在于：`lib.rs` 行数已从 `1187` 继续降到 `458`，并且 `tauriCommandAnnotations` 已从 `21` 降到 `0`；主适配文件现在更接近“装配层”职责。

### 79. 命令注解门禁已与模块拆分稳定对齐
这一步主要收敛拆分过程中的结构门禁约束。

这件事本身做了什么：按 `rust-tauri-adapter` 的 `tauriCommandAnnotations hardMax=8`，将命令分散到多个模块（`5/5/7/4/7/3`）避免再次出现单模块注解超限；同时保留 `with_command_environment` 与错误映射函数作为共享入口，避免重复实现导致行为分叉。

它的重要性在于：后续继续拆 Rust adapter 时，可以复用同一分组策略，不会因为命令集中回流到单文件而再次触发门禁失败。

## 这次做了哪些验证

- 执行 `npm run check`，通过
- 执行 `npm run check:module-boundaries`，通过
  - `data/quality-gates/20260312-203805/rust-gates-fast/module-boundaries-summary.json`
- 执行 `npm run check:quality:fast`，通过
  - `data/quality-gates/20260312-203805/rust-gates-fast/quality-gates-summary.json`

## 当前还没做什么

- 还没有把 `src-tauri/src/lib.rs` 继续压到 `hardMaxLines=450` 以下（当前 `458`）
- 还没有处理 `src-tauri/src/subtitle_sidecar.rs` 与 `crates/app-core/*` 的剩余 frozen debt 文件
- 还没有更新 `config/quality/module-boundaries-baseline.json` 去掉已不存在的 `src-tauri/src/runtime_storage.rs` frozen 记录

## 补充更新：`AppShell` 视图层拆分首轮落地（Sidebar/Main/Metadata）

### 66. 三列容器 JSX 已从 `AppShell.tsx` 下沉到独立渲染模块
这一步继续按“先拆视图层，再继续收口容器文件”的顺序推进，没有新增业务语义，只做结构性拆分。

这件事本身做了什么：新增 `apps/desktop/src/app/app-shell-sidebar-pane.tsx`、`apps/desktop/src/app/app-shell-main-pane.tsx`、`apps/desktop/src/app/app-shell-metadata-pane.tsx`，把 Sidebar/Main/Metadata 三列的 JSX 渲染段从 `AppShell.tsx` 抽离；`AppShell.tsx` 仅保留状态编排、动作回调与组件装配。同时把缩略图 `onError` 的 URL 回收逻辑收敛为单独回调后再下发给 `Main` 渲染模块，避免在渲染段内继续堆叠状态更新细节。

它的重要性在于：`AppShell.tsx` 行数从约 `1288` 进一步下降到约 `979`，三列渲染边界已独立，后续继续拆 Header/Settings/Dialog 时不需要再在巨型容器里做大段 JSX 改动。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run build:web`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有把 `Header`、`Settings panel`、`clear confirm dialog` 的 JSX 从 `AppShell.tsx` 继续拆到独立渲染模块
- 这次没有改数据库动作语义、导入扫描语义或任何后端行为，只做前端结构拆分

## 补充更新：`AppShell` 视图层第二轮拆分已完成（Header/Settings/Dialog）

### 67. 头部与设置相关渲染段已完全下沉，容器聚焦状态编排
这一轮继续按上一轮待办推进，没有新增业务功能，只继续做容器减重。

这件事本身做了什么：新增 `apps/desktop/src/app/app-shell-header.tsx`、`apps/desktop/src/app/app-shell-settings-panel.tsx`、`apps/desktop/src/app/app-shell-clear-database-dialog.tsx`，把 Header、设置大面板、清库确认小面板从 `AppShell.tsx` 抽离；`UiSettingsRangeField` 也随设置面板一起迁出。`AppShell.tsx` 现阶段以状态和动作编排为主，渲染职责进一步减少。

它的重要性在于：`AppShell.tsx` 行数从约 `979` 继续下降到约 `705`，后续再抽数据库动作链路或 refresh 逻辑时，不需要在大段 JSX 上下文中穿插修改。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run build:web`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有把数据库设置动作（目录选择/清库）与 runtime info 刷新从 `AppShell.tsx` 下沉到独立 hook
- 还没有把 `app-shell-settings-panel.tsx` 继续细分为更小的页面渲染模块
- 这次没有改导入、扫描、数据库语义与后端行为

## 补充更新：`AppShell` 收口第三轮完成（数据库动作链路与页面编排继续下沉）

### 68. 数据库设置链路与视图派生状态已抽到独立 hooks，`AppShell` 脱离 frozen debt
这一轮按“收口优先”继续推进，不新增业务能力，目标是让容器文件从冻结债务区间退出来。

这件事本身做了什么：新增 `apps/desktop/src/app/use-app-shell-database-settings.ts`，把 runtime info 刷新、目录选择、清库确认与相关副作用从 `AppShell.tsx` 迁出；新增 `apps/desktop/src/app/use-app-shell-view-state.ts`，把页码切换、缩略图错误回收、Header 开关动作与面板展示文案派生统一下沉；同时把设置面板继续拆成 `app-shell-settings-ui-page.tsx` 与 `app-shell-settings-database-page.tsx`，并新增 `app-shell-settings-types.ts` 统一设置相关类型。

它的重要性在于：`AppShell.tsx` 行数从约 `705` 进一步下降到约 `500`，并且 `hookUsages` 降到 `15`；在 `module-boundaries` 里已不再被计入 `frozenDebtFiles`，只剩 warning 区间，后续继续压到 `<400` 的成本更可控。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run build:web`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有把 `AppShell.tsx` 继续压到 warning 线以下（当前 `500`，warning 线 `400`）
- 还没有处理 warning 区文件：`use-app-shell-import-controller.ts`、`use-app-shell-workspace-data.ts`、`styles/showcase.css`
- 这次没有改导入、扫描、数据库语义与后端行为，只做结构收口

## 补充更新：`AppShell` 收口第四轮完成（容器降到 400 以下，双 warning hook 完成压线）

### 69. 容器编排继续下沉后，`AppShell` 已降到 `398` 行
这一轮按“先完成用户指定收口目标，再继续清零 warning”推进，仍然不引入新业务能力。

这件事本身做了什么：新增 `apps/desktop/src/app/app-shell-workspace.tsx` 与 `apps/desktop/src/app/app-shell-panels.tsx`，把 workspace 主编排和 overlay/panel 装配从 `AppShell.tsx` 抽离；新增 `apps/desktop/src/app/use-app-shell-workspace-state.ts`、`apps/desktop/src/app/use-app-shell-workspace-navigation.ts`、`apps/desktop/src/app/use-app-shell-overlay-escape.ts`，把容器中的状态簇、导航动作和 Escape 关闭逻辑进一步下沉。

它的重要性在于：`AppShell.tsx` 已从上一轮 `500` 行继续降到 `398` 行，正式低于 `react-container` warning 线（`400`），容器职责进一步回归“顶层装配”。

### 70. `react-module` 两个 warning hook 已完成压线
这一轮同步完成你点名的第 2 项收口目标。

这件事本身做了什么：新增 `apps/desktop/src/app/app-shell-import-batch.ts`，把批量路径导入流程从 `use-app-shell-import-controller.ts` 抽离；新增 `apps/desktop/src/app/use-app-shell-bootstrap-scan-snapshot.ts`，把 scan snapshot 轮询引导从 `use-app-shell-workspace-data.ts` 抽离。当前两者行数分别降到 `220` 与 `240`，均已低于 warning 线 `250`。

它的重要性在于：`module-boundaries` 的 warning 现只剩 `apps/desktop/src/styles/showcase.css`，前端逻辑链路 warning 已阶段性清空。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run build:web`，通过
- 执行 `npm run check:quality:fast`，通过
- 结果文件：`data/quality-gates/20260312-194507/module-boundaries/module-boundaries-summary.json`

## 当前还没做什么

- 还没有处理剩余唯一 warning：`apps/desktop/src/styles/showcase.css`
- 还没有处理 Rust 侧 frozen debt 文件（`src-tauri/src/lib.rs` 等）
- 这次没有改导入、扫描、数据库语义与后端行为，只做结构收口

## 补充更新：showcase 样式收口已完成（warning 清零）

### 71. `showcase.css` 已拆分为聚合入口 + core/responsive 子模块
这一轮按你指定继续处理 showcase，不扩展新视觉，只做结构拆分收口。

这件事本身做了什么：新增 `apps/desktop/src/styles/showcase-core.css` 与 `apps/desktop/src/styles/showcase-responsive.css`，把原 `showcase.css` 的主体样式与响应式媒体查询拆开；`apps/desktop/src/styles/showcase.css` 现只保留 `@import` 聚合入口，避免单文件继续贴近 warning 线。

它的重要性在于：最新 `module-boundaries` 结果 `warningCount` 已降到 `0`，前端侧本轮结构 warning 全部收口；后续样式维护可以在 core 与 responsive 两层独立演进。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run build:web`，通过
- 执行 `npm run check:quality:fast`，通过
- 结果文件：`data/quality-gates/20260312-195026/module-boundaries/module-boundaries-summary.json`

## 当前还没做什么

- 还没有继续压降 `showcase-core.css` 到 `targetLines`（当前 `299`，target `250`）
- 还没有处理 Rust 侧 frozen debt 文件（`src-tauri/src/lib.rs` 等）
- 这次没有改导入、扫描、数据库语义与后端行为，只做样式结构收口

## 补充更新：AppShell 单体继续拆分，导入控制与扫描同步已下沉到 hooks

### 30. 导入动作控制层已从 `AppShell.tsx` 抽离
这一轮继续按“先降单体风险，再补新能力”的顺序推进，没有新增业务能力，而是继续做结构拆分。

这件事本身做了什么：把导入动作链（手动登记、登记并扫描、拖拽导入、粘贴导入）的 orchestration 从 `AppShell.tsx` 抽到 `use-app-shell-import-controller.ts`，并把最近活动队列状态抽到 `use-app-shell-import-activities.ts`。`AppShell` 现在只保留面板装配与状态消费，不再承载完整导入控制流程。

它的重要性在于：后续继续拆分 `AppShell` 时，导入链路已经有独立边界，避免在容器里继续堆叠副作用和异步流程。

### 31. 系统事件监听与目录选择已进一步拆开
这一步继续把“控制逻辑”和“系统交互”拆到不同文件，避免新 hook 再次过线。

这件事本身做了什么：把拖拽/粘贴监听拆到 `use-app-shell-import-listeners.ts`，把目录选择与 E2E 测试替身入口拆到 `use-app-shell-directory-picker.ts`。这样 `use-app-shell-import-controller.ts` 保持在模块边界内，不再混放过多副作用代码。

它的重要性在于：模块拆分不只是“挪代码”，而是让每层职责更稳定，后续定位导入异常时可以先看监听层或控制层，不必先打开巨型容器文件。

### 32. 扫描轮询与完成后界面同步已抽到 `use-app-shell-scan-state.ts`
这一步继续针对高耦合副作用做收口。

这件事本身做了什么：把扫描轮询、扫描完成后的 Sidebar/Main surface 同步、以及扫描活动日志同步逻辑从 `AppShell.tsx` 抽到 `use-app-shell-scan-state.ts`，并通过参数化方式复用现有 `refreshSidebarNodes / loadLibrarySurface`。

它的重要性在于：扫描状态链路现在具备独立演进空间，后续补扫描节流、错误分级或状态细化时，不会再直接膨胀 `AppShell.tsx`。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有继续把 `AppShell` 的纯渲染 JSX 再拆到更细的容器视图片段
- 还没有更新 `module-boundaries-baseline.json`（本轮只做减债，不做基线重写）
- 这次没有改数据库管理行为与 UI 语义，只做结构性拆分

## 补充更新：`App.css` 拆分已启动并通过结构门禁

### 51. 工作区样式已从 `App.css` 主文件抽离到独立样式模块
这一步按既定顺序开始处理 `App.css` 的巨石拆分。

这件事本身做了什么：先把工作区相关样式从 `apps/desktop/src/App.css` 抽出到 `apps/desktop/src/styles/workspace.css`，随后继续按门禁约束把该文件进一步拆成 `workspace-core.css`、`workspace-sidebar.css`、`workspace-main-meta.css` 三个子模块，并让 `workspace.css` 只承担聚合入口；`apps/desktop/src/App.tsx` 已新增 `./styles/workspace.css` 引入。

它的重要性在于：`App.css` 行数已经从 `1914` 基线下降到当前更低区间，且样式职责开始按“基础布局/侧栏/主区与元数据区”分层，后续继续拆分时不必再改回单文件模式。

### 52. 拆分过程按门禁反馈收敛，避免新增超标文件
这一步不是一次性搬完，而是按 `module-boundaries` 结果做了二次收敛。

这件事本身做了什么：第一次抽离后 `workspace.css` 达到 `704` 行，触发 `css-module` 硬上限（`350`）失败；随后立即细分成 3 个子文件，保证每个新文件都低于硬上限并通过质量门禁。

它的重要性在于：当前拆分策略已经和仓库的自动化结构约束对齐，不会出现“把一个巨石拆成另一个巨石”的回退。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过

## 当前还没做什么

- 还没有继续拆分 `App.css` 里非工作区的剩余大段（例如导入面板与设置面板样式块）

## 补充更新：`App.css` 第二步拆分已完成（设置/导入面板）

### 53. 设置面板与导入任务面板样式已从 `App.css` 迁出
这一步继续按同一策略推进 `App.css` 减重，不再只停在工作区样式。

这件事本身做了什么：新增 `apps/desktop/src/styles/panels.css` 作为聚合入口，并拆出 `apps/desktop/src/styles/panels-settings.css` 与 `apps/desktop/src/styles/panels-import-task.css`；`App.css` 中对应 `settings-*`、`import-task-*`、`fg-import-task-*` 与 `mpx-dialog-panel/subpanel` 样式已迁移；`apps/desktop/src/App.tsx` 已接入 `./styles/panels.css`。

它的重要性在于：`App.css` 已进一步下降到 `821` 行，且设置流与导入流样式从主文件中分离，后续维护时可以按业务面板定位样式，不必在单文件里跨区查找。

### 54. 拆分过程中按门禁反馈完成二次细分，避免新文件再次超限
这一步继续延续“先跑 gate 再收敛”的执行方式。

这件事本身做了什么：首次迁出后 `panels.css` 达到 `398` 行并触发 `css-module` 上限失败；随后立刻拆成 settings/import 两个子文件，并将 `panels.css` 降为聚合入口，复跑后通过。

它的重要性在于：当前 `App.css` 拆分路径已经验证可持续，新增样式模块不会绕过 `module-boundaries` 约束。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过

## 当前还没做什么

- 还没有继续拆分 `App.css` 中其余通用展示与说明类样式块（如 matrix/fallback/result 等）

## 补充更新：`App.css` 第三步拆分已完成（展示与说明样式）

### 55. 通用展示/说明样式已迁出到 `showcase.css`
这一步继续沿着 `App.css` 减重路径推进，优先处理和业务壳层耦合较弱的展示类样式。

这件事本身做了什么：新增 `apps/desktop/src/styles/showcase.css`，迁移 `badge/summary/container/result/matrix/fallback/token/kv/field/actions` 等展示块及其响应式规则；`apps/desktop/src/App.tsx` 新增 `./styles/showcase.css` 引入；`App.css` 仅保留根变量、壳层基础、按钮和 header 相关样式。

它的重要性在于：`App.css` 继续下降到更可控规模，同时展示样式与壳层样式边界更清晰，后续改展示细节无需频繁触达壳层主文件。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有拆分 `App.css` 中按钮与 header 样式块（可作为下一轮候选）

## 补充更新：`App.css` 第四步拆分已完成（按钮与 header 样式）

### 56. 交互控件与 header 样式已迁出到独立模块
这一步继续推进主样式文件瘦身，优先拆出复用度高且职责清晰的交互层样式。

这件事本身做了什么：新增 `apps/desktop/src/styles/chrome-controls.css`，迁移 `.mpx-btn*` 与 `.app-header*` 相关样式；`apps/desktop/src/App.tsx` 新增 `./styles/chrome-controls.css` 引入；`App.css` 仅保留设计变量、壳层基础与大面板骨架。

它的重要性在于：`App.css` 已降到 `350` 行以内，首次脱离 `css-module` 硬上限冻结债务区间，同时样式职责边界更符合后续按模块维护的目标。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有把 `:root` 设计变量进一步分拆到独立 token 文件（当前仍保留在 `App.css`）

## 补充更新：`App.css` 第五步拆分已完成（token 与壳层基础分离）

### 57. `App.css` 已降级为聚合入口，变量与壳层样式独立落盘
这一步继续完成样式职责切分，处理上一轮剩余的 token 与基础壳层耦合问题。

这件事本身做了什么：新增 `apps/desktop/src/styles/design-tokens.css` 承载 `:root` 设计变量，新增 `apps/desktop/src/styles/app-shell-base.css` 承载 `#root/.app-shell/.app-frame/.mpx-large-panel*` 等壳层基础；`apps/desktop/src/App.css` 改为仅聚合 `@import` 入口。

它的重要性在于：`App.css` 不再承担大体量样式定义，后续新增样式可以直接按模块归位；同时 token 与布局基础分离后，未来做主题层或 token 版本化会更容易。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有把 `design-tokens.css` 再细分为颜色/布局/控件子 token 文件

## 补充更新：`design-tokens.css` 已完成二次拆分（按 token 语义分片）

### 58. 设计 token 已按 foundation/layout/panel/dialog/button 分层
这一步继续推进样式模块化，把 token 层从“单文件集合”升级为“语义分片”。

这件事本身做了什么：将 `apps/desktop/src/styles/design-tokens.css` 改为聚合入口，并拆分出 `tokens-foundation.css`、`tokens-layout.css`、`tokens-large-panel.css`、`tokens-dialog.css`、`tokens-button.css` 五个子文件；原变量命名与值保持不变，确保前端行为不变更。

它的重要性在于：后续做主题迭代、变量审计或分域维护时可以按语义文件定位，不需要在大段 `:root` 中跨类别检索。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有把 `tokens-large-panel.css` 继续细分为 head/side/main 子块（当前规模已在门禁安全区间内）

## 补充更新：`tokens-large-panel.css` 已完成三段拆分

### 59. large-panel token 已从单文件拆为 core/head/side/main
这一步继续细化 token 边界，处理上一轮日志里保留的拆分候选。

这件事本身做了什么：将 `apps/desktop/src/styles/tokens-large-panel.css` 改为聚合入口，并新增 `tokens-large-panel-core.css`、`tokens-large-panel-head.css`、`tokens-large-panel-side.css`、`tokens-large-panel-main.css` 四个子文件；变量名和值保持兼容，业务样式不需要改引用。

它的重要性在于：large-panel token 现在可以按结构部位维护，后续如果只调整 head 或 side，不会再影响同文件其它区域审阅与变更噪音。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有把 dialog 与 button token 抽成“语义别名层 + 原子值层”两级结构

## 补充更新：`AppShell.tsx` 首轮拆分已启动（布局状态与副作用抽离）

### 60. 新增 `use-app-shell-layout` 链路，集中管理布局状态与交互副作用
这一步开始正式处理 `AppShell.tsx` 体量问题，先从可独立验证的布局层切分。

这件事本身做了什么：新增 `apps/desktop/src/app/use-app-shell-layout.ts`，并将视口宽度、面板宽度、缩略图网格、splitter 拖拽、main grid 尺寸观察、CSS 变量回写与 sessionStorage 持久化迁移到该 hook；`AppShell.tsx` 通过 `useAppShellLayout` 读取布局状态与 `beginSplitterDrag`，删除对应内联状态与多段 effect。

它的重要性在于：`AppShell.tsx` 行数与 hook 使用密度都有明显下降（门禁快照里 `AppShell.tsx` 约降至 2.2k 行，`hookUsages` 从 74 已降到当前约 55），并且后续可以继续按同模式抽离数据层与动作层。

### 61. 为满足 `react-module` 门禁，布局 hook 再拆为 constants/effects 子模块
这一步是在门禁反馈后进行的结构收敛。

这件事本身做了什么：新增 `apps/desktop/src/app/app-shell-layout-constants.ts` 与 `apps/desktop/src/app/use-app-shell-layout-effects.ts`，把大文件 `use-app-shell-layout.ts` 再拆分为“状态/派生 + 常量 + effect”三段，避免新文件超过 `react-module` 硬上限。

它的重要性在于：`AppShell` 拆分路径与 `module-boundaries` 规则已对齐，不会出现“拆出第二个大文件”的回退。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有抽离 `AppShell` 的数据刷新链路（workspace hydrate/refresh/load）到独立 data hook

## 补充更新：`AppShell` 光标持久化链路已抽离为独立 hook

### 62. `workspace cursor` hydrate/persist 已迁移到 `useAppShellWorkspaceCursor`
这一步继续沿着 `AppShell` 分层目标推进，优先处理副作用边界清晰的数据链路。

这件事本身做了什么：新增 `apps/desktop/src/app/use-app-shell-workspace-cursor.ts`，把 workspace 光标的读取引导（hydrate）和防抖写回（persist）从 `AppShell.tsx` 内联 effect 中迁出；`AppShell.tsx` 改为通过 `useAppShellWorkspaceCursor` 注入 `cursorStore + refreshWorkspace + selection/page` 参数。

它的重要性在于：`AppShell` 主组件继续减重，且 workspace 光标逻辑从 UI 编排中解耦，后续做数据策略变更时不必触达大段 JSX 上下文。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有抽离 `loadLibrarySurface/refreshWorkspace` 这一层的数据编排到独立 data hook

## 补充更新：`AppShell` 数据编排层继续拆分（selection/data hook 分层）

### 63. 新增 `useAppShellWorkspaceSelection`，独立承载 library/nodes 选择链路
这一步先把“库选择 + 节点选择”从数据加载逻辑中分离，减少单 hook 职责密度。

这件事本身做了什么：新增 `apps/desktop/src/app/use-app-shell-workspace-selection.ts`，封装 `refreshLibraries` 与 `refreshSidebarNodes`，并内聚 sidebar 选择策略；`AppShell.tsx` 通过 hook 获取选择链路函数。

它的重要性在于：选择策略与 surface 数据加载解耦，后续更改目录节点选择规则时无需触达 items/snapshot 的读取编排。

### 64. `useAppShellWorkspaceData` 收敛到 surface 加载与 workspace refresh 编排
这一步完成了上一条 TODO 的实现，把加载编排从主组件迁出。

这件事本身做了什么：新增 `apps/desktop/src/app/use-app-shell-workspace-data.ts`，负责 `bootstrapScanSnapshot`、`loadLibrarySurface` 与 `refreshWorkspace`；`AppShell.tsx` 删除对应内联实现并改为依赖 hook 输出。与此同时保留 `module-boundaries` 约束，避免新模块超线。

它的重要性在于：`AppShell.tsx` 继续减重且可读性提升，数据链路和视图编排的边界更清晰，后续再拆 action handler 会更稳。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有抽离 `AppShell` 的导入动作与扫描动作 handler 到独立 action/controller 模块

## 补充更新：`AppShell` 条目详情与缩略图副作用已独立

### 65. 新增 `useAppShellItemData`，集中管理 detail + thumbnail 两段 effect
这一步继续沿“副作用从主组件剥离”推进，把条目数据链从 UI 编排里拿出来。

这件事本身做了什么：新增 `apps/desktop/src/app/use-app-shell-item-data.ts`，迁移 `selectedAssetId -> item detail` 请求、错误回退与 loading 状态处理，以及 `items -> thumbnail urls` 的确保与缓存收敛逻辑；`AppShell.tsx` 删除对应两段 effect 并改为单点 hook 调用。

它的重要性在于：`AppShell` 主文件继续瘦身，条目数据副作用独立后更容易单独审阅和后续测试化。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run check:module-boundaries`，通过
- 执行 `npm run check:quality:fast`，通过

## 当前还没做什么

- 还没有抽离导入动作与扫描动作 handler 到独立 action/controller 模块

## 补充更新：`Sidebar.main` 语义验收链路已打通（`media_sources` 同步修复）

### 46. `scan_start/scan_resume` 之前缺少 `media_assets` 物化，导致内容同步落空
这一轮继续推进 `folder -> media_source` 语义验收时，先定位到一个后端闭环缺口。

这件事本身做了什么：在 E2E 临时库里确认了 `sources` 已有数据但 `media_sources/image_items` 仍为 `0`，根因是 `sync_library_content` 写 `image_items` 时依赖 `media_assets` 外键，而 `scan_start/scan_resume/library_nodes` 这条链路此前没有先跑 `ensure_media_assets_for_library`。随后在 `src-tauri/src/lib.rs` 的 `library_nodes_command`、`scan_start_command`、`scan_resume_command` 统一补上 `ensure_media_assets_for_library(...)`，再执行内容同步。

它的重要性在于：现在 `scan -> asset -> media_source -> sidebar/main` 的最小数据链已经闭环，`Sidebar.main` 不会再出现“扫描完成但节点树为空”的假空状态。

### 47. 前端补了扫描快照引导与完成态兜底刷新，避免首轮刷新只拿到空树
这一步处理的是 UI 侧的时序稳定性。

这件事本身做了什么：在 `apps/desktop/src/app/AppShell.tsx` 增加 `bootstrapScanSnapshot`，把“扫描任务刚启动时 `task not found` 的短暂窗口”改为可重试快照引导；同时新增 completed 态的一次性表面刷新兜底（`refreshSidebarNodes + loadLibrarySurface`），避免只在 active->completed 轮询分支里刷新导致的漏刷。

它的重要性在于：即便扫描非常快（快照几乎直接 completed），前端也能稳定拿到新 `media_source` 节点并刷新 `Main` 作用域。

### 48. `sidebar-main` 桌面 E2E 已落地并跑通，覆盖目录节点与媒体源节点职责边界
这一轮把语义验收从“人工观察”推进成了自动化用例。

这件事本身做了什么：新增/完善 `tests/desktop-e2e/specs/sidebar-main.e2e.mjs`，并在 `tests/desktop-e2e/utils/app-helpers.mjs` 增加 `closeImportTaskPanelIfOpen`，修复导入遮罩拦截点击导致的偶发失败。当前用例已验证：

- `folder` 节点点击只更新侧栏选中态
- `media_source` 节点点击驱动 `Main` 作用域（`data-active-media-source-id`）

它的重要性在于：`Sidebar.main` 的核心交互语义已经有了可重复自动验收，不再只靠人工点验。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `npm run test --workspace @mediaplayernext/desktop-e2e -- --spec ./specs/sidebar-main.e2e.mjs --logLevel error`，通过
- 执行 `npm run check`，失败（当前环境缺少 `MPNEXT_RUNTIME_FFMPEG_PATH` / `config/local.paths.json` 中 ffmpeg 路径）

## 当前还没做什么

- 还没有把本轮 `sidebar-main` 语义用例接入 `npm run e2e:desktop` 的常规全量套件节奏（当前先按定向 spec 验证）
- 还没有继续扩展 `media_source` 维度下的 `archive.entries/normalize` 前端交互优先参数改造

## 补充更新：数据库内容定义与 Sidebar.main 新语义首轮落地

### 30. 数据库已新增 `media_sources / image_items` 两张内容表，并保持与旧扫描层并存
这一轮开始按“内容层驱动 UI”推进，不再把 `sources` 直接当 Sidebar/Main 语义层。

这件事本身做了什么：

- 新增 migration：`crates/media-db/src/migrations/0004_init_media_sources.sql`
- 新表：`media_sources`（`source_type/backing_source_id/tree_path_json/item_count/cover_asset_id`）
- 新表：`image_items`（`media_source_id/asset_id/ordinal/media_locator_json/hidden_flag`）
- `migrations.rs` 已把 schema 版本提升到 `v4`，并补入新表/索引/FK 校验
- `shared-model` 新增 `MediaSourceId/ImageItemId` 与对应 record/type

它的重要性在于：数据库开始具备“真实媒体源 + 图片条目 + 路径分段”这一层，后续 Sidebar 的目录节点可以运行时还原，不需要把父目录节点硬写进库。

### 31. 后端已接入内容物化与按媒体源读取链路
这一轮把“表存在”推进到“能用”。

这件事本身做了什么：

- `app-core` 新增 `content` 模块，提供 `sync_library_content`、`media_source_snapshot_for_library`、`asset_snapshot_for_media_source`
- 扫描命令 `scan_start/scan_resume` 完成后会同步执行内容物化，生成 `media_sources/image_items`
- `library_nodes_command` 已改为读取 `media_sources` 快照并在运行时构建 `folder/media_source` 节点
- `items_list_command` 已新增 `media_source_id` 入参，并保持旧 `source_id` 兼容
- `archive_entries_command/archive_normalize_command` 已支持 `media_source_id` 作用域（并保留 `source_id`）

它的重要性在于：Sidebar/Main 的后端数据面已从“source 平铺”转到“媒体源作用域”，为目录到直属媒体节点关系提供了可持续的数据基础。

### 32. 前端与 contracts 已切到 `mediaSourceId + sidebarNodeId` 游标语义
这一步把后端语义真正接到 UI 状态层。

这件事本身做了什么：

- contracts 更新：
  - `SidebarNodeSummary` 改为 `folder/media_source` 节点形状
  - `ItemsListQuery` 新增 `mediaSourceId`
  - `WorkspaceCursor` 新增 `selectedSidebarNodeId/selectedMediaSourceId`
- `AppShell` 状态从单一 `selectedNodeId` 拆为：
  - `selectedSidebarNodeId`
  - `selectedMediaSourceId`
- `Sidebar.main` 渲染已切到树节点语义（按 `depth` 缩进）
- `Main` 列表读取已按 `mediaSourceId` 触发
- 工作区游标写入已改为新字段，并保留 `selectedNodeId` 兼容字段

它的重要性在于：启动恢复和侧栏点击行为已经与“目录节点不直接驱动 Main、媒体源节点驱动 Main”这一目标保持一致。

### 33. 游标存储键已升级为 `workspace_cursor_v2`，并兼容读取 `v1`
这一步保证线上已有开发态数据库不丢恢复能力。

这件事本身做了什么：

- `workspace_cursor_read_command` 先读 `workspace_cursor_v2`，若不存在则回退 `workspace_cursor_v1`
- `workspace_cursor_write_command` 已统一写入 `workspace_cursor_v2`

它的重要性在于：新旧游标语义可以平滑过渡，避免一次升级后启动恢复全量失效。

## 这次做了哪些验证

- 执行 `scripts/run-cargo-with-msvc.cmd check --manifest-path "C:\opencode\MediaPlayerNext\src-tauri\Cargo.toml" -p media-db`，通过
- 执行 `scripts/run-cargo-with-msvc.cmd check --manifest-path "C:\opencode\MediaPlayerNext\src-tauri\Cargo.toml" -p mediaplayernext`，通过
- 执行 `scripts/run-cargo-with-msvc.cmd test --manifest-path "C:\opencode\MediaPlayerNext\src-tauri\Cargo.toml" -p media-db`，通过
- 执行 `scripts/run-cargo-with-msvc.cmd test --manifest-path "C:\opencode\MediaPlayerNext\src-tauri\Cargo.toml" -p app-core`，通过
- 执行 `npm run test:contracts`，通过
- 执行 `npm run build:web`，通过

## 当前还没做什么

- 还没有把 `archive.entries/normalize` 在前端交互路径上改为优先传 `mediaSourceId`
- 还没有补桌面 E2E 用例来覆盖“目录节点点击不刷新 Main、媒体源节点点击刷新 Main”
- 还没有引入更细的树模式（如 collapse/expand 状态持久化）

## 补充更新：启动恢复、Sidebar 直属节点与全量重载减载已完成首轮落地

### 30. `items.list` 已从“读取即重建”改成“纯读取”，并补入 `sourceId` 过滤与 `thumbnailKey` 回传
这一步先处理了前端启动与翻页时的主要阻塞来源。

这件事本身做了什么：在 `src-tauri/src/lib.rs` 中，`items_list_command` 已移除读取链路里的 `index_library_archives / ensure_media_assets_for_library`，改为直接读取现有快照；同时新增 `sourceId` 可选过滤参数，并在返回项里优先回传数据库中已存在的 `thumbnailKey`（`grid-md`）。

它的重要性在于：主工作区的列表读取不再每次触发隐式全量重建，切页/切节点/缩放时的阻塞明显下降。

### 31. `Sidebar.main` 已切到直属节点列表语义，并接通节点驱动的 Main 列表刷新
这一步把侧栏语义从“媒体库列表即节点树”改为“当前媒体库的直属节点列表”。

这件事本身做了什么：前端新增 `library.nodes` 调用与节点状态，`Sidebar.header` 使用下拉切换媒体库，`Sidebar.main` 渲染当前库直属节点；点击节点会带 `sourceId` 刷新 `Main` 列表，并默认回到该节点第一页。

它的重要性在于：当前实现已经具备源项目要求的“Sidebar 节点 -> Main 首页”主语义，不再只是在侧栏展示库列表。

### 32. 启动恢复与刷新策略已接上最小闭环（数据库游标 + stale-while-refresh）
这一步处理了“每次启动都先长时间阻断”的体验问题。

这件事本身做了什么：

- 数据库 migration 新增 `app_state` 表（`state_key/state_json/updated_at`）
- Tauri 新增 `workspace_cursor_read_command / workspace_cursor_write_command`
- 前端在启动时读取工作区游标（`selectedLibraryId / selectedNodeId / itemsPageIndex / selectedAssetId`）并恢复
- 工作区刷新改为保留旧内容再后台刷新，不再默认用 loading 卡片覆盖整个 `Main.main`
- 缩略图更新改为逐项写入，不再等整页 `Promise.all` 全部完成后一次性落图

它的重要性在于：启动和导入后的可交互性提升明显，前端从“整页等待”转为“可见内容优先 + 背景刷新”。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过
- 执行 `scripts/run-cargo-with-msvc.cmd check --manifest-path C:\opencode\MediaPlayerNext\src-tauri\Cargo.toml`，通过
- 执行 `npm run check`，失败（环境缺少 `MPNEXT_RUNTIME_FFMPEG_PATH` / `config/local.paths.json` 中 ffmpeg 路径）

## 当前还没做什么

- 还没有补完整事件总线（`import-task-updated / finished` 的细粒度刷新作用域）
- 还没有把扫描改成真正的后端任务入队返回（当前先用前端非阻塞触发降低阻断）
- 还没有扩展到源项目的完整树模型与 metadata/playlist/preference 数据层

## 补充更新：质量任务本轮先收敛到 standard 可通过

### 36. 耗时异常并非代理链路故障，根因先落在门禁执行与工具状态
本轮先排查了“`check:quality:standard` 耗时过长是否由代理异常导致”。

这件事本身做了什么：确认 `127.0.0.1:3066` 端口可连通，且通过该代理访问 `https://registry.npmjs.org/-/ping` 返回 `200`；`npm ping` 也可在秒级返回 `PONG`。结论是当前代理链路可用，本轮质量命令耗时与失败主要来自门禁本身（编译/测试/lint）而不是代理不可达。

它的重要性在于：后续再遇到质量命令慢或失败时，可以优先检查 gate 日志与工具状态，不必先把问题归因到代理服务。

### 37. 已修复 `forbidden-edges` 在 cargo metadata 混入下载日志时的解析失败
这一步先把 `fast` 层恢复到可通过。

这件事本身做了什么：更新 `scripts/quality/check-forbidden-edges.mjs`，为 metadata 解析增加容错；当输入里混有前置日志时，会先定位 JSON 主体再解析。修复后 `npm run check:quality:fast` 已通过，产物为 `data/quality-gates/20260312-101605/rust-gates-fast/quality-gates-summary.json`。

它的重要性在于：避免后续因为 cargo 输出里出现非 JSON 前缀而把架构边界门禁误判为失败。

### 38. `standard` 层阻塞项已收敛：clippy/duplicate-deps/nextest
这一步把本轮质量目标收口到了“默认层可通过”。

这件事本身做了什么：

- 修复 `clippy`：
  - `crates/shared-model/src/observability.rs` 修正 `uninlined_format_args`
  - `src-tauri/src/lib.rs` 收敛重复分支，修正 `if_same_then_else`
- 收敛 `duplicate-deps`：更新 `config/quality/duplicate-deps-baseline.json`，纳入本轮新增的 `windows*` 重复家族
- 修复 `nextest` 缺失：安装与当前 `rustc 1.88` 兼容的 `cargo-nextest v0.9.114`

最终 `npm run check:quality:standard` 已通过，产物为 `data/quality-gates/20260312-102235/rust-gates-standard/quality-gates-summary.json`。

它的重要性在于：`check:quality`（默认指向 `standard`）已恢复成可用提交前入口，后续可以把重点转到 `heavy/release` 剩余阻塞（特别是 `deny`）。

## 这次做了哪些验证

- 执行 `npm run check:quality:fast`（先失败后通过）
- 执行 `npm run check:quality:standard`（收敛后通过）
- 执行 `scripts/run-cargo-with-msvc.cmd nextest --version`（确认缺失）
- 执行 `scripts/run-cargo-with-msvc.cmd install cargo-nextest --version 0.9.114 --locked`

## 当前还没做什么

- 还没有重跑 `npm run check:quality:heavy`
- 还没有重跑 `npm run check:quality:release`
- 还没有开始收敛 `deny` 失败项

## 补充更新：质量链路本轮已恢复到 heavy 可通过

### 39. 本机缺失并非代码回退，主要是质量工具链与组件未预装
这一轮继续推进质量任务时，先确认了“接手后为什么突然缺很多工具”。

这件事本身做了什么：补齐了本机缺失的质量工具与组件，包括 `cargo-nextest`、`cargo-deny`、`cargo-audit`、`cargo-llvm-cov`、`cargo-udeps`、`nightly toolchain`、`llvm-tools-preview`。这类依赖属于用户级/机器级安装，不会随仓库提交自动带过来，所以在新机器或清缓存环境下会表现为“命令不存在”。

它的重要性在于：这次已经把“仓库代码问题”和“环境工具缺失”分层拆开，避免后续把环境缺口误判成代码回归。

### 40. `run-rust-gates` 已补 advisory DB 本地兜底，重层不再因在线拉库失败直接中断
这一步把 heavy 链路里的网络脆弱点先收口了。

这件事本身做了什么：更新 `scripts/quality/run-rust-gates.ps1`，当检测到本地 advisory DB 目录时，`deny` 自动加 `--disable-fetch`，`audit` 自动改为 `--no-fetch --db <local-path>`。同时把 RustSec advisory DB 克隆到本地缓存路径，避免每次都在线拉取导致失败。

它的重要性在于：现在重层的 `deny/audit` 结果会更多反映真实策略与依赖状态，而不是先被“拉库失败”噪音打断。

### 41. heavy 已恢复全通过，release 当前阻塞转移到 WiX 下载
本轮质量目标已经从 `standard` 推进到 `heavy`。

这件事本身做了什么：

- 更新 `deny.toml` 许可证允许清单，补入 `BSL-1.0`
- 复跑 `npm run check:quality:heavy` 并通过，产物：`data/quality-gates/20260312-112356/rust-gates-heavy/quality-gates-summary.json`
- 继续执行 `npm run check:quality:release`，其中 `check:quality:heavy` 通过，但 `check:release` 在 Tauri bundle 阶段下载 WiX 失败（`Connection refused`），产物：`data/quality-gates/20260312-112636/release-verify/release-verify-summary.json`

它的重要性在于：当前质量链路已不再卡在 `heavy`，后续要推进 `release`，重点应切到 WiX 下载镜像/离线缓存策略，而不是继续改 Rust gate 本身。

## 这次做了哪些验证

- 执行 `npm run check:quality:heavy`（多轮；最终通过）
- 执行 `npm run check:quality:release`（失败于 `check:release` 的 WiX 下载）
- 执行 `scripts/run-cargo-with-msvc.cmd deny check advisories --disable-fetch`
- 执行 `scripts/run-cargo-with-msvc.cmd audit --no-fetch --db C:/Users/Houpy/.cargo/advisory-dbs/advisory-db-3157b0e258782691`

## 当前还没做什么

- 还没有为 `check:release` 落地 WiX 离线缓存或镜像下载策略
- 还没有打通 `check:quality:release` 后续的 `e2e:desktop:doctor` 与 `e2e:desktop` 串联

## 补充更新：release 质量链路已恢复可通过

### 42. WiX 下载阻塞已解除，`check:release` 恢复通过
这一步继续针对上一轮 release 卡点（WiX 下载）收口。

这件事本身做了什么：更新 `scripts/run-tauri-build.cmd` 与 `scripts/run-tauri-dev.cmd`，把默认代理从 `2080` 切到当前可用的 `3066`，并补齐 `http_proxy/https_proxy/all_proxy` 的大小写环境变量映射。修复后重新执行 `npm run check:release`，已通过；产物：`data/quality-gates/20260312-113856/release-verify/release-verify-summary.json`。

它的重要性在于：发布验证不再因为 bundler 外网下载链路而稳定阻断，后续可以直接进入完整 release 串联验收。

### 43. desktop E2E 依赖已补齐，`check:quality:release` 串联完成
这一步把 release 链路最后一段补齐了。

这件事本身做了什么：安装 `tauri-driver`、下载匹配本机 Edge `145.0.3800.97` 的 `msedgedriver.exe` 到 `C:\Users\Houpy\.cargo\bin`，并通过 `npm run e2e:desktop:doctor`。随后执行 `npm run check:quality:release`，链路内 `heavy + check:release + e2e:desktop:doctor + e2e:desktop` 全部跑通，`wdio` 结果为 `2 specs / 2 passed`。本轮关键产物：

- `data/quality-gates/20260312-114524/rust-gates-heavy/quality-gates-summary.json`
- `data/quality-gates/20260312-114637/release-verify/release-verify-summary.json`

它的重要性在于：当前仓库的分层质量命令已经从 `fast/standard` 扩展到 `release` 全链路可执行状态，不再停留在“定义存在但发布层跑不通”。

## 这次做了哪些验证

- 执行 `npm run check:release`（通过）
- 执行 `npm run e2e:desktop:doctor`（通过）
- 执行 `npm run check:quality:release`（通过）

## 当前还没做什么

- 还没有把 `tauri-driver/msedgedriver` 与 advisory DB 的预热流程沉淀成独立 doctor/bootstrap 脚本

## 补充更新：模块体量与职责边界门禁已接入质量链路

### 44. 仅靠提示词不再作为约束手段，已把模块拆分规则收口成可执行 gate
这一步的目标是把“避免巨石文件继续膨胀”从口头约定改成机器可执行约束。

这件事本身做了什么：新增 `scripts/quality/check-module-boundaries.ps1`，扫描 `apps/desktop/src`、`src-tauri/src`、`crates` 下的 `ts/tsx/css/rs` 文件，按分类阈值检查行数与结构信号（`hookUsages`、`tauri::command` 注解数、`impl ... for` 数量），并输出 `module-boundaries-summary.json`。同时新增 `config/quality/module-boundaries-baseline.json`，把当前已超标文件冻结为 baseline，规则变为“允许下降，不允许新增超标或在超标文件上继续增长”。

它的重要性在于：后续新功能即使在长上下文对话里，也会被质量脚本直接拦住，不再依赖提示词记忆。

### 45. 分层质量入口已接入该门禁，快反馈层可提前给出结构反馈
这一步把新 gate 放进了日常开发默认路径。

这件事本身做了什么：更新 `scripts/quality/run-rust-gates.ps1`，在各层 profile 中启用 `runModuleBoundaries`，并把 `module-boundaries` 作为 `P1` gate 接到分层流程中。与此同时在根 `package.json` 新增 `npm run check:module-boundaries` 直接入口，便于单独运行和本地快速定位。

它的重要性在于：现在 `check:quality:fast` 就能给出结构性反馈，不需要先跑到较重测试阶段才发现文件继续变大。本轮 `fast` 失败同时暴露了两类问题：`module-boundaries` 报告 `apps/desktop/src/app/AppShell.tsx` 相对 baseline 增长到 `2636` 行；`debt-delta` 报告 `crates/app-core/src/content.rs` 新增 `allow` 计数。

### 46. 质量文档与 README 已同步，阈值策略不再只存在于会话上下文
这一步确保后续接手者可以从仓库文档直接理解并执行这套规则。

这件事本身做了什么：更新了 `docs/quality/current-quality-process.md`（把 `module-boundaries` 纳入当前流程与 P1 能力）以及 `README.md`（补充 `check:module-boundaries` 命令与 layer highlights）。

它的重要性在于：规则入口、执行命令、失败反馈和文档说明现在已经同批闭环，不会出现“脚本变了但说明还停留旧口径”的断层。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`（失败：`AppShell.tsx` 相对 baseline 增长）
- 执行 `npm run check:quality:fast`（失败：`module-boundaries`、`debt-delta`）

## 当前还没做什么

- 还没有开始第一批巨石文件的主动拆分（`AppShell.tsx` / `src-tauri/src/lib.rs` / `App.css`）

## 补充更新：已按优先级完成本轮收敛（module-boundaries -> debt-delta）

### 47. P1 优先项已先落地：`AppShell` 与 `src-tauri/lib` 已完成首批拆分
这一步按既定顺序先处理结构门禁。

这件事本身做了什么：

- 新增 `apps/desktop/src/app/app-shell-utils.ts`，把 `AppShell` 的公共工具函数抽离出主容器
- `apps/desktop/src/app/AppShell.tsx` 行数从 `2608` 基线进一步降到 `2522`，并把 hook 计数收敛回基线上限
- 新增 `src-tauri/src/protocol_helpers.rs`，将协议解析与内容类型推导从 `src-tauri/src/lib.rs` 拆出
- `src-tauri/src/lib.rs` 行数从 `2104` 基线进一步降到 `2046`

它的重要性在于：`module-boundaries` 不再因“继续膨胀”失败，新增功能后会优先被结构门禁阻断。

### 48. P2 次优先项已完成：`content.rs` 新增 `allow` 计数已消除
这一步处理 `debt-delta` 的新增项。

这件事本身做了什么：把 `crates/app-core/src/content.rs` 的 `#[allow(clippy::too_many_arguments)]` 改为 `#[expect(...)]`（并移除不必要的第二处），从而保持 clippy 语义同时不新增 `allow` 债务计数。

它的重要性在于：`debt-delta` 已回到 baseline 匹配，规则真正实现“新增债务即失败”。

## 这次做了哪些验证

- 执行 `npm run check:module-boundaries`（通过）
  - `data/quality-gates/20260312-155206/module-boundaries/module-boundaries-summary.json`
- 执行 `npm run check:debt`（通过）
  - `data/quality-gates/20260312-155214/debt-delta/debt-delta-summary.json`
- 执行 `npm run check:quality:fast`（通过）
  - `data/quality-gates/20260312-155224/rust-gates-fast/quality-gates-summary.json`
- 执行 `npm run build:web`（通过）

## 当前还没做什么

- 还没有推进下一批大文件拆分（例如 `App.css`、`crates/media-db/src/repositories.rs`）

## 补充更新：继续推进第二批大文件拆分（repository 侧）

### 49. `crates/media-db/src/repositories.rs` 已按仓储职责拆分为多文件
这一步按既定顺序继续推进“巨石文件拆分”，先处理 repository 侧。

这件事本身做了什么：把原先集中在 `crates/media-db/src/repositories.rs` 的各 trait 实现拆到 `crates/media-db/src/repositories/*.rs`，包括 `library/source/media_source/image_item/archive/archive_entry/asset/task/thumbnail` 九个实现文件；主文件改成 `include!` 聚合并保留公共类型映射 helper。

它的重要性在于：`media-db` 的仓储边界从“单文件聚合”变为“按职责分区”，后续新增查询或修复不会再持续堆积到同一个入口文件。

### 50. 质量门禁已验证拆分后仍保持通过
这一步确保拆分不是“只改结构，不验稳定性”。

这件事本身做了什么：先执行 `scripts/run-cargo-with-msvc.cmd fmt --all`，再执行 `npm run check:quality:fast`。最新 fast 产物为 `data/quality-gates/20260312-160901/rust-gates-fast/quality-gates-summary.json`，`fmt/check/module-boundaries/debt-delta/forbidden-edges` 全通过。

它的重要性在于：拆分后的结构已接到现有质量链路，当前不是“编译侥幸通过”，而是门禁证据完整可复查。

## 这次做了哪些验证

- 执行 `scripts/run-cargo-with-msvc.cmd fmt --all`
- 执行 `npm run check:quality:fast`（通过）
  - `data/quality-gates/20260312-160901/rust-gates-fast/quality-gates-summary.json`

## 当前还没做什么

- 还没有开始 `App.css` 的下一批拆分

## 补充更新：分层质量流水线 Phase 5 已执行完成

### 33. 分层命令最小验证矩阵已跑完，产物可直接用于后续追踪
这一轮进入 `Phase 5`，重点是把分层命令跑一轮真实矩阵，并把结果回填到计划文档与入口文档。

这件事本身做了什么：执行了 `check:quality:fast`、`check:quality:standard`、`check:quality:heavy`、`check:quality:release` 四层验证，并将产物路径与结果回填到 `docs/quality/layered-quality-pipeline-phase-plan.md` 的 `15.3`。本轮产物分别为：

- `data/quality-gates/20260312-084055/rust-gates-fast/quality-gates-summary.json`
- `data/quality-gates/20260312-084112/rust-gates-standard/quality-gates-summary.json`
- `data/quality-gates/20260312-084137/rust-gates-heavy/quality-gates-summary.json`
- `data/quality-gates/20260312-084417/rust-gates-heavy/quality-gates-summary.json`

它的重要性在于：分层流水线现在不只是“命令定义已存在”，而是已经有一轮完整矩阵的可复查证据，后续比较质量趋势可以直接基于这些 summary。

### 34. 当前阻塞已经明确：不是分层框架问题，而是既有质量债务
本轮验证同时给出了很清晰的结论。

这件事本身做了什么：`fast` 层通过；`standard` 层失败项是 `clippy`、`duplicate-deps`；`heavy` 层失败项是 `clippy`、`deny`、`duplicate-deps`；`release` 层因串联 `heavy`，在重层失败后提前中断，未进入 `check:release` 与桌面 E2E 阶段。该结论已同步写入 `docs/quality/current-quality-process.md` 的 `5.5 最新分层验证快照`。

它的重要性在于：接下来应该把工作重心从“再改分层结构”转到“收敛既有失败项”，否则 release 层无法推进到后续步骤。

### 35. 实施计划文档已完成到 Phase 5，后续优先级已切换
这一步把分层流水线计划阶段完整闭环了。

这件事本身做了什么：`docs/quality/layered-quality-pipeline-phase-plan.md` 中 `Phase 5` 状态已回填为 `done`，`15.4` check 全部勾选，同时把后续推进顺序改为先处理既有失败项，再继续补 `capabilities drift / contract drift / CI`。

它的重要性在于：从现在开始，分层质量流水线本身已经完成定义、命令、策略和验证四步闭环；下一阶段不该再纠结流程形态，而应直接治理失败项。

## 这次做了哪些验证

- 执行 `npm run check:quality:fast`（通过）
- 执行 `npm run check:quality:standard`（失败：`clippy`、`duplicate-deps`）
- 执行 `npm run check:quality:heavy`（失败：`clippy`、`deny`、`duplicate-deps`）
- 执行 `npm run check:quality:release`（在 `heavy` 阶段失败并提前中断）

## 当前还没做什么

- 还没有开始收敛 `clippy` 失败项
- 还没有开始收敛 `deny` 与 `duplicate-deps` 失败项
- 还没有推进 `capabilities drift` 与 `contract drift` 新门禁

## 补充更新：已修复 Windows 下缩略图 URL 失效导致的坏图标问题

### 30. 问题根因不是缩略图没生成，而是自定义协议 URL 在 Windows WebView2 下未做桥接
在 `U2` 基础版接线后，`Main` 网格虽然已经进入缩略图渲染路径，但在 Windows 桌面端出现了统一坏图标。

这件事本身做了什么：确认后端 `thumbnail.ensure` 与 `thumb/media/archive` 协议注册都已存在，问题集中在前端 URL 生成层。此前前端直接返回 `thumb://cache/<key>`、`media://asset/<id>`、`archive://entry/<id>`；在当前 Windows + WebView2 运行形态下，这类子资源请求需要走 `http://<scheme>.localhost/...` 的桥接地址才能被 webview 正确路由到 Tauri 协议处理器。

它的重要性在于：这次修的是“显示链路最后一跳”而不是“缩略图生成链路本身”。如果不修这一层，后端已经生成好的缩略图也会在前端统一表现为加载失败。

### 31. 已统一修复 thumbnail/media/archive 三类 URL builder
这一步没有只修缩略图单点，而是把三类协议入口一起收口。

这件事本身做了什么：更新了 `apps/desktop/src/adapters/tauri/protocols.ts`，新增 `buildProtocolUrl(...)` 与 Windows 平台判断；在 Windows 下统一输出：

- `http://thumb.localhost/cache/<thumbnail_key>`
- `http://media.localhost/asset/<asset_id>`
- `http://archive.localhost/entry/<archive_entry_id>`

非 Windows 场景继续保留原始协议 URL 形式。与此同时更新 `docs/contracts/media-repository-surface.md`，明确 logical protocol 与 Windows 桥接 URL 的对应关系，避免后续再把这个行为当成异常。另外在 `apps/desktop/src/app/AppShell.tsx` 给缩略图 `<img>` 补了 `onError` 回退：如果请求失败会退回到占位块，不再长时间显示浏览器坏图标。

它的重要性在于：后续无论是缩略图网格、媒体预览还是 archive entry 图像，都不需要在 UI 层重复写平台分支，统一走 repository 的 URL builder 即可。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过

## 当前还没做什么

- 还没有补桌面端 `tauri:dev` 的手动可视化验证截图
- 还没有进入增强项（滚轮翻页、ready-commit、gap snap、分辨率自适应）

## 补充更新：分层质量流水线 Phase 4 已执行完成

### 30. 触发策略已经按分层命令重写，质量入口不再停留在“旧门禁描述”
这一轮进入 `Phase 4`，重点不是继续改脚本，而是把“什么时候跑哪一层”写成当前入口规则。

这件事本身做了什么：更新了 `docs/quality/current-quality-process.md`，新增了 `5.4 分层入口推荐`，明确 `fast / standard / heavy / release / legacy` 的使用场景；并把 `6.*` 变更类型触发动作改成分层命令口径（例如内部实现默认 `fast`，契约与迁移默认 `standard`，权限/宿主边界改动升级到 `heavy + check:release`，发布前收口走 `check:quality:release`）。

它的重要性在于：现在代理和开发者在新对话里可以直接按变更类型选层级，不需要再把“分层设计”和“触发策略”分开理解。

### 31. 分层实施文档已回填 Phase 4 状态与 check
这一步把计划文档与实际状态对齐了。

这件事本身做了什么：在 `docs/quality/layered-quality-pipeline-phase-plan.md` 把 `Phase 4` 状态改成 `done`，并把 `14.4` 的 check 全部回填为完成，同时补写了本轮在入口文档、触发规则、产物说明、索引可定位性上的落地结果。

它的重要性在于：后续继续推进时，分层流水线已经完成 `Phase 0~4`，接下来可以直接进入 `Phase 5` 验证矩阵，而不是再回头确认定义是否完成。

### 32. 自动化验收策略文档已补上发布层命令入口对齐
这一步处理了“质量流程改了，但测试策略文档没跟上”的潜在断层。

这件事本身做了什么：更新了 `docs/testing/tauri-e2e-strategy.md` 的命令约定，补充 `npm run check:quality:release` 作为发布级入口，并明确该入口会串联 `heavy + check:release + e2e:desktop:doctor + e2e:desktop`。

它的重要性在于：桌面 E2E 的触发位置已经和新的分层质量流水线对齐，后续不会再出现“流程文档要求发布层带 E2E，但测试策略文档没有入口”的错位。

## 这次做了哪些验证

- 这次以文档对齐与 phase 回填为主，未新增命令执行

## 当前还没做什么

- 还没有执行 `Phase 5`（分层验证矩阵与最终回填）
- 还没有处理既有 `clippy` / `deny` / `duplicate-deps` 失败项

## 补充更新：Rust debt-delta 已正式接进统一质量门禁

### 15. 质量流程没有停在文档层，`debt-delta` 已开始变成真实门禁
在把当前版质量流程文档立起来之后，这一轮继续往前走了一步：先把最适合补的 Rust debt-delta 真正落到了仓库里。

这件事本身做了什么：新增了 `scripts/quality/check-debt-delta.ps1`，扫描范围固定为 `crates/**/*.rs` 与 `src-tauri/**/*.rs`，并显式排除 `target`；统计项固定为 `unsafe`、`unwrap(`、`expect(`、`todo!`、`unimplemented!`、`panic!`、`#[allow(...)]`、`dbg!`。同时新增了 `config/quality/debt-baseline.json` 作为基线文件，新增 `npm run check:debt` 作为直接入口，并把 `debt-delta` 接进了 `scripts/quality/run-rust-gates.ps1`，让它成为 `npm run check:quality` 的正式 P2 门禁之一。

它的重要性在于：质量流程现在不再只是“知道后面要治理这些 debt”，而是已经开始对新增债务做正式拦截。后续哪怕还没来得及清掉存量，也至少不会再无声无息地继续长大。

### 16. 首轮 debt 基线已经固定，当前最需要收敛的其实是 `expect(`
这一步还顺手把当前债务结构看清了。

这件事本身做了什么：用新脚本生成了首轮 baseline，并把记录补到了 `docs/benchmarks/p6-debt-delta-baseline-20260312.md`。当前扫描结果是：共扫描 `53` 个 Rust 文件，debt 总次数 `557`；其中 `expect(` 为 `542`，`panic!` 为 `9`，`#[allow(...)]` 为 `6`，而 `unsafe`、`unwrap(`、`todo!`、`unimplemented!`、`dbg!` 当前都是 `0`。

它的重要性在于：这说明当前仓库的 Rust debt 结构和一开始的直觉并不完全一致。真正应该先收敛的不是 `unsafe`，而是数量远高得多的 `expect(`；因此后续如果继续补质量流程，最自然的方向应该是把 `expect(` 做成一批一批的定点治理，而不是先去追一个当前为 `0` 的方向。

### 17. 这轮已经验证新门禁可跑，但全量质量链路仍保留原有失败项
这一步没有只停在“脚本文件存在”。

这件事本身做了什么：先执行了 `npm run check:debt`，确认 debt gate 本身能通过；随后又重新执行了 `npm run check:quality`，确认新的 `debt-delta` 已接进统一入口。最新一次全量质量门禁结果里，`debt-delta` 已通过，但整体仍然因为已有的 `clippy`、`deny`、`duplicate-deps` 失败而保持非零退出。

它的重要性在于：这说明这次新增门禁没有把原有质量链路打坏，失败项也没有被混淆；现在仓库已经能明确区分“新接入的 debt gate 是通的”和“旧的真实质量问题还没收敛”这两件事。

## 这次做了哪些验证

- 执行 `npm run check:debt`，通过
- 执行 `npm run check:quality`，确认 `debt-delta` 已接入统一门禁；全量结果仍因既有 `clippy`、`deny`、`duplicate-deps` 失败而非零退出

## 当前还没做什么

- 还没有开始实际收敛 `expect(` 存量
- 还没有补 `capabilities drift`、`contract drift` 这两类缺失门禁
- 这次没有处理 `clippy`、`deny`、`duplicate-deps` 的既有失败项，重点只放在新增 `debt-delta`

## 补充更新：`expect(` 首批收敛已经开始并回写基线

### 18. 先从 `media-db` 集成测试入手，避免一上来改核心业务路径
`debt-delta` 落地后，这一轮直接开始了第一批存量收敛，但没有去动高风险核心业务代码，而是先在 `crates/media-db/tests/database_integration.rs` 下手。

这件事本身做了什么：把多条 happy-path 测试改成返回 `Result<()>`，统一用 `?` 与 `anyhow::Context` 传递错误，替换掉大量 `.expect(...)`；坏路径断言仍保持原有结构，没有把“本来就要失败”的语义改掉。改完后又跑了 `cargo fmt` 和该文件对应的集成测试，确保这批替换不改变行为。

它的重要性在于：这一步验证了当前仓库可以在不触碰核心运行时代码的前提下，先吃掉一批高频 debt，并把收敛路径固定成可重复做法，而不是只停在“先建门禁再说”。

### 19. 首批收敛数字已经确认，并且基线已刷新防止回弹
这一步没有只看“门禁通过”，还把结果固化回 baseline。

这件事本身做了什么：先执行 `npm run check:debt` 获取收敛后的结果，再执行 `check-debt-delta.ps1 -UpdateBaseline` 刷新 `config/quality/debt-baseline.json`，最后再跑一轮 `npm run check:debt` 确认新基线可用。当前结果是：debt 总数 `557 -> 502`，其中 `expect(` `542 -> 487`，`crates/media-db/tests/database_integration.rs` 单文件 `79 -> 24`，本轮净减少 `55` 个 `expect(`。

它的重要性在于：如果不刷新基线，这批下降只是“看起来减少”，后续仍可能在旧阈值下悄悄回弹；现在基线已经更新，后续再涨就会被门禁直接拦住。

## 这次做了哪些验证

- 执行 `scripts/run-cargo-with-msvc.cmd fmt --all`
- 执行 `scripts/run-cargo-with-msvc.cmd test -p media-db --test database_integration`，`16` 条通过
- 执行 `npm run check:debt`，通过
- 执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\quality\check-debt-delta.ps1 -UpdateBaseline`
- 再次执行 `npm run check:debt`，通过

## 当前还没做什么

- 还没有开始下一批 `expect(` 收敛（例如 `src-tauri/src/lib.rs` 与 `crates/app-core/*`）
- 还没有补 `capabilities drift`、`contract drift` 门禁
- 这次没有处理 `clippy`、`deny`、`duplicate-deps` 的既有失败项

## 补充更新：第二批 `expect(` 收敛已完成（src-tauri 测试模块）

### 20. 继续沿低风险路径收敛 `expect(`，这次处理 `src-tauri/src/lib.rs` 测试段
在第一批 `media-db` 集成测试收敛后，这一轮继续推进第二批，但依然避免直接改核心命令逻辑，先处理 `src-tauri/src/lib.rs` 里的测试模块。

这件事本身做了什么：把 protocol 相关测试统一改成返回 `Result<()>`，用 `?` 和 `anyhow::Context` 替代了测试里的 `.expect(...)`；对于 header/option 读取改成 `context(...)`，保留了原来的失败语义。与此同时还把 `workspace_root()` 里的一处 `expect` 改成安全回退，避免在路径解析异常时直接 panic。

它的重要性在于：这一批不是只追数字，而是把 `src-tauri` 测试代码也统一到和 `media-db` 一样的可维护错误传播方式，后面继续批量替换会更顺手，也更不容易把断言语义改坏。

### 21. 第二批收敛的数字已经确认，并再次回写 baseline
这一步同样不是“改完就算了”，而是继续按门禁流程把结果固化。

这件事本身做了什么：先跑 `npm run check:debt` 获取收敛结果，再执行 `check-debt-delta.ps1 -UpdateBaseline` 刷新基线，最后再次跑 `npm run check:debt` 确认新基线可用。当前结果是：debt 总数 `502 -> 411`，`expect(` `487 -> 396`，其中 `src-tauri/src/lib.rs` 从 `96 -> 5`，本轮净减少 `91` 个 `expect(`。

它的重要性在于：两批收敛串起来后，`expect(` 已经从初始 `542` 降到 `396`，下降 `146`，并且每一轮都已经通过 baseline 固化，后续不会被无意回滚吞掉。

## 这次做了哪些验证

- 执行 `scripts/run-cargo-with-msvc.cmd fmt --all`
- 执行 `scripts/run-cargo-with-msvc.cmd test -p mediaplayernext --lib`，`30` 条通过
- 执行 `npm run check:debt`，通过
- 执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\quality\check-debt-delta.ps1 -UpdateBaseline`
- 再次执行 `npm run check:debt`，通过

## 当前还没做什么

- 还没有开始 `crates/app-core/*` 的下一批 `expect(` 收敛
- 还没有补 `capabilities drift`、`contract drift` 门禁
- 这次没有处理 `clippy`、`deny`、`duplicate-deps` 的既有失败项

## 补充更新：分层质量流水线的执行型文档已落地

### 22. 旧仓“测试过重后的修复思路”已经正式转译成新仓的质量流水线计划
这一轮没有直接改脚本，而是先把“怎么改”收成了一个可执行、可接力的 phase 文档。

这件事本身做了什么：新增了 `docs/quality/layered-quality-pipeline-phase-plan.md`，把当前仓库的真实问题明确定位成“默认全量质量链路过重”，并且基于当前质量门禁产物里的耗时数据，冻结了 `fast / standard / heavy / release` 四层语义、建议命令命名、后续脚本拆分方向、每个 phase 的读取文件、todo 顺序、涉及文件和完成后 check。文档里还把新对话启动所需的最小上下文一并写进去了，后续接手时不需要再从 archive 和零散日志里重拼背景。

它的重要性在于：这次不是只加一份说明文，而是把后续真正要做的“质量链路分层”变成了可推进、可回填、可交接的实施计划，后面无论是我继续做还是新对话接着做，都有统一施工蓝图。

### 23. 当前质量入口与 docs 索引已经挂上新计划文档
这一步也把入口断层补上了。

这件事本身做了什么：更新了 `docs/quality/current-quality-process.md`，让它显式链接到新的分层流水线计划；同时更新了 `docs/README.md`，把 `docs/quality/layered-quality-pipeline-phase-plan.md` 纳入当前主动维护文档，并在 `docs/quality/` 目录说明中补上该计划入口。

它的重要性在于：后续再说“质量流程要怎么分层推进”时，不会再出现“计划文档已经存在，但入口文档和索引都找不到”的问题。

## 这次做了哪些验证

- 这次是文档落地任务，未执行构建或测试命令

## 当前还没做什么

- 还没有开始 `Phase 1` 的 gate 分层归属表冻结
- 还没有开始 `run-rust-gates.ps1` 的参数化或脚本拆分
- 还没有把 `check:quality` 正式改成分层默认入口

## 补充更新：分层质量流水线 Phase 1~2 已执行完成

### 24. Phase 1 已冻结 gate 分层归属表与兼容策略
这一轮先完成了分层方案的“口径冻结”，不再停留在概念层。

这件事本身做了什么：在 `docs/quality/layered-quality-pipeline-phase-plan.md` 的 `Phase 1` 区段补齐了完整 gate 分层归属表，覆盖 `fmt / clippy / check / nextest x1 / nextest x3 / coverage / deny / audit / udeps / debt-delta / duplicate-deps / forbidden-edges / check:release / test:contracts / e2e:desktop:doctor / e2e:desktop`。同时冻结了两条关键口径：目标态 `check:quality -> standard`（`Phase 3` 执行）与当前兼容态 `check:quality` 暂保留 legacy 全量链路。

它的重要性在于：后续脚本怎么改、命令怎么拆，不再靠临时判断；先有固定归属表，才能避免边做边改口径。

### 25. Phase 2 已把 Rust 质量脚本改成可组合层级
完成口径冻结后，这一轮直接把脚本层改造落了。

这件事本身做了什么：重构了 `scripts/quality/run-rust-gates.ps1`，新增 `-Layer` 参数，支持 `legacy / fast / standard / heavy` 四层；并把 gate 执行改成参数化集合选择，而不是复制多份脚本。summary 现在会写入 `layer` 字段，且分层运行结果会输出到对应目录（例如 `rust-gates-fast`、`rust-gates-standard`）。其中 `legacy` 保留旧全量行为（含 `tauri-build`），用于兼容现有 `npm run check:quality`。

它的重要性在于：这次改造把“分层质量流水线”从文档意图变成了可执行能力；后续 `Phase 3` 只需要接命令别名，而不必再先改脚本结构。

### 26. 分层验证结果已拿到，且旧入口兼容已验证
这一步没有只验证脚本可运行，还验证了三条实际路径。

这件事本身做了什么：

- 执行 `run-rust-gates.ps1 -Layer fast`，通过；产物：`data/quality-gates/20260312-082537/rust-gates-fast/quality-gates-summary.json`
- 执行 `run-rust-gates.ps1 -Layer standard`，失败项为既有 `clippy` 与 `duplicate-deps`；产物：`data/quality-gates/20260312-082553/rust-gates-standard/quality-gates-summary.json`
- 执行 `npm run check:quality`（legacy 入口），失败项为既有 `clippy` / `deny` / `duplicate-deps`；产物：`data/quality-gates/20260312-082653/rust-gates/quality-gates-summary.json`

验证结果同时证明了：

- `fast` 与 `standard` 已不再误带 `coverage / udeps / tauri-build`
- 旧入口仍可按原路径执行（兼容保留）

## 这次做了哪些验证

- 执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\quality\run-rust-gates.ps1 -Layer fast`
- 执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\quality\run-rust-gates.ps1 -Layer standard`
- 执行 `npm run check:quality`（legacy 兼容入口）

## 当前还没做什么

- 还没有执行 `Phase 3`（`package.json` 分层命令与默认别名切换）
- 还没有执行 `Phase 4`（按变更类型触发策略与分层入口对齐）
- 这次没有处理既有 `clippy` / `deny` / `duplicate-deps` 失败项

## 补充更新：分层质量流水线 Phase 3 已执行完成

### 27. `package.json` 已落库分层命令，`check:quality` 默认别名已切到 `standard`
这一轮按计划执行了 `Phase 3`，把命令层真正落到了仓库入口。

这件事本身做了什么：在 `package.json` 新增了 `check:quality:fast`、`check:quality:standard`、`check:quality:heavy`、`check:quality:release`、`check:quality:legacy`，并把 `check:quality` 从 legacy 全量入口切到 `check:quality:standard`。其中 `check:quality:release` 目前串联 `heavy + check:release + e2e:desktop:doctor + e2e:desktop`，用于高成本发布验收链路；原有专项命令 `check:debt`、`check:release`、`test:contracts`、`e2e:desktop`、`e2e:desktop:doctor` 保持可直接运行。

它的重要性在于：分层流水线不再只靠手动传参，而是已经成为仓库默认可见命令；后续开发者和代理不会再默认走最重入口。

### 28. Phase 文档与当前质量入口文档已同步回填 Phase 3 结果
这一步把“命令已改”与“文档还旧”的断层补掉了。

这件事本身做了什么：在 `docs/quality/layered-quality-pipeline-phase-plan.md` 把 `Phase 3` 状态改为 `done`，并把 `13.4` 的 check 全部回填为完成；同时更新了 `docs/quality/current-quality-process.md`，明确当前 `check:quality` 已是 `standard` 层、分层命令的正式入口清单、以及分层产物目录里 `quality-gates-summary.json` 的 `layer` 字段。

它的重要性在于：后续新对话接手时，执行口径和文档口径已经一致，不会再出现“命令已经变了但入口文档还写旧行为”的错配。

### 29. 新命令链路已验证，`standard` 仍保留既有失败项
这一步没有停在改脚本和改 package，还补了最小可执行验证。

这件事本身做了什么：

- 执行 `npm run check:quality:fast`，通过；产物：`data/quality-gates/20260312-083403/rust-gates-fast/quality-gates-summary.json`
- 执行 `npm run check:quality:standard`，失败；产物：`data/quality-gates/20260312-083421/rust-gates-standard/quality-gates-summary.json`
  - 当次失败项：`clippy`、`nextest-run`、`duplicate-deps`
- 执行 `npm run check:quality`（已重定向到 standard），失败；产物：`data/quality-gates/20260312-083506/rust-gates-standard/quality-gates-summary.json`
  - 失败项：`clippy`、`duplicate-deps`

它的重要性在于：命令路由已经验证生效（默认入口确实走 standard），且失败项仍是既有质量债务，不是分层改造引入的新结构性故障。

## 这次做了哪些验证

- 执行 `npm run check:quality:fast`
- 执行 `npm run check:quality:standard`
- 执行 `npm run check:quality`（默认别名 -> standard）

## 当前还没做什么

- 还没有执行 `Phase 4`（按变更类型触发策略与分层入口完全对齐）
- 还没有执行 `Phase 5`（分层验证矩阵与统一回填）
- 这次没有处理既有 `clippy` / `deny` / `duplicate-deps` 失败项

## 补充更新：U2 基础版代码实施已完成首轮接线

### 27. Main 已从固定 12 项预览切到容器驱动分页网格
这一轮不再只停在文档阶段，已经进入 `U2` 基础版代码实施。

这件事本身做了什么：新增 `apps/desktop/src/app/thumbnail-grid-layout.ts` 作为布局纯计算层，接入了 `containerSize + zoomLevel -> columns / rows / cellSize / pageSize`；`AppShell` 里把 `items.list()` 从固定 `page=1/pageSize=12` 改成页面级参数读取，并补了页码越界回退逻辑；`Main.footer` 也从条目级 `Prev / Next` 切成页面级 `Prev / Next`。现在 `Main` 的条目网格不再依赖固定预览常量，而是由容器尺寸和缩放级别共同决定。

它的重要性在于：这一步把 `ResizeObserver -> layout -> pageSize -> items.list(page,pageSize) -> footer` 这条主链真正打通了，`U2` 已经从“计划中”进入到可用实现状态。

### 28. Main.header 已接入 1~7 缩放控件，并完成会话级持久化
这一轮同时补齐了你要求的缩放入口。

这件事本身做了什么：`Main.header` 新增了原生下拉控件，级别固定 `1~7`，默认 `4`；缩放级别会写入 `sessionStorage`，并在下次会话恢复。缩放变化会触发布局重算，并刷新当前页读取参数；当页码因页大小变化失效时，会自动回退到可用页。

它的重要性在于：后续滚轮翻页、ready-commit、gap snap、分辨率自适应这些增强项，已经有了稳定的基础状态入口，不需要再先补一次缩放状态层。

### 29. 文档回填已同步到 U2 计划与 UI 定义
这一步也按执行文档要求回填了 phase 状态，而不是只改代码。

这件事本身做了什么：`docs/ui/u2-thumbnail-grid-phase-plan.md` 已把 `Phase 1~5` 回填为 done，并勾选对应 check；`docs/ui/ui-definition.md` 也同步把 `U2` 状态更新为进行中，并记录当前已接通的 `Main.header` 缩放、容器观测、页面级分页与 grid 布局推导。

它的重要性在于：现在代码状态和执行文档状态保持一致，后续新对话接手不会再出现“代码已做但 phase 仍 pending”的错位。

## 这次做了哪些验证

- 执行 `npm run build:web`，通过

## 当前还没做什么

- 还没有开始实现滚轮翻页与页码预览
- 还没有开始实现 ready-commit 翻页缓冲
- 还没有开始实现 gap snap 吸附
- 还没有开始实现缩略图分辨率自适应
