# media-playback

负责 `B7` 阶段的播放后端适配与最小会话编排。

当前已落地的首批能力：

- `ffprobe` JSON 输出解析与元数据汇总
- `ffmpeg` 抽帧参数构造与 `-progress` 解析
- `mpv` 最小启动参数构造
- 文件型播放会话的磁盘持久化与状态更新

当前还未落地：

- 真正的 `mpv` IPC 控制
- 高级播放列表与多窗口编排
- 复杂转码调度
